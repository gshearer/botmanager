stock_plugin = shared_library('stock',
  'stock.c',
  include_directories : [inc_include, include_directories('.')],
  dependencies : [m_dep],
  override_options : ['b_lundef=false'],
  install : true,
)
