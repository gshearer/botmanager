// botmanager — MIT
// Kraken Spot authenticated REST endpoints (orders, balances, fills).
// KR-3 ships an empty translation unit; KR-4 populates the typed
// wrappers and the form-urlencoded body builders.
#define KR_INTERNAL
#include "kraken.h"
