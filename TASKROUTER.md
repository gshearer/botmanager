## TASKROUTER.md — Go to your task and minimize input token spend

This is the project's **single routing index**: each task maps to the
smallest set of docs that lets you execute it. When you add or move a
module, folder, feature, or doc, update the relevant row here **in the
same commit** (per `AGENTS.md §MUST`). Keep rows task-shaped ("I want
to X") and point at the per-directory `AGENTS.md` leaf rather than
listing every file — the leaf carries the detail and is maintained
alongside its code.

| Your Task | Read These Files |
|-----------|------------------|
| **Get oriented in a fresh context (project briefing)** | `PRIMER.md` |
| **Decide how much verification a change owes** (a test? the sanitizers? a live gate? or just a clean build?) | `AGENTS.md §Verification` — the ladder, and the rule that **over-verification is a defect of the same family as under-verification**. Key on how the change fails: how loudly, how soon you would know, how hard to undo. ⚠ **Do not calibrate from the register**: `TODO.md` and `ACHIEVED.md` carry ~1,000 mentions of gates and red-first proofs, all of it downstream of the 2026-08 `SKILL-CONFORM-AUDIT`, which ran before `posixc-pro` had a proportionality rule. Those rows are provenance, not a standard to match |
| **Write, refactor or review ANY C in this tree** | The **`posixc-pro` skill** (`.claude/skills/posixc-pro/SKILL.md`) — load it *before* opening a `.c`/`.h`. It is the authority on idiom, on the banned-functions table and on the modern-interface defaults, and `AGENTS.md §MUST` cites it rather than restating it. ⚠ It is **its own git repo** and does not travel with a botmanager clone, so the two rules that bite most often here are written out: ⭑ **a bounded string copy is `strlcpy(dst, src, sizeof dst)`** — `snprintf` formats and does not copy (it is *not* banned, so the tree's ~968 `snprintf(dst, sizeof dst, "%s", src)` pure-copies are a mismatch with the preferred form, not a defect: convert one when you are already editing it, never as a sweep), while **`strncpy` IS banned** as a fixed-width *field* function — no NUL on truncation, pads the whole buffer — legal only where you want that padding and tagged `// posixc-pro: strncpy — <why>` when you do. ⚠⚠ Converting one is where the mistake lives: `strncpy(d, s, SZ - 1)` becomes `strlcpy(d, s, SZ)` and **the `- 1` must be dropped**, or the buffer silently truncates a byte early forever, with no warning and no test that would catch it. ⭑ **Never add a check that cannot fail** — the same rule PA-5 deleted 171 branches for (`include/alloc.h`), and `strlcpy`'s truncation return is only worth testing where truncation actually matters. ⚠ `strlcpy` needs a `-std=gnu*` build: glibc gates it behind `__USE_MISC`, so under any `-std=c*` it vanishes and gcc's implicit-declaration error helpfully suggests `strncpy`, which is how the old idiom reasserts itself. Measured 2026-08-13, glibc 2.44 / gcc 16.2.1. ⭑⭑ **The build is HARDENED since `SC-HARDEN` (08-15)** and one flag changes how you write C here: `-D_FORTIFY_SOURCE=3` makes `read`/`write` `warn_unused_result`, and ⚠⚠ a `(void)` cast does **not** silence that attribute — read the return or use a helper that does (core's eventfd wake/drain is `util_evfd_wake()` / `util_evfd_drain()`). The clean-build set is `-Wall -Wextra -Wpedantic -Wformat-security`; ⛔ `-Wformat=2` is refused **upstream** (skill v1.9.1) because its `-Wformat-nonliteral` fires on every `fmt`-passing wrapper. ⭐ **The skill is at v1.10.0 since 2026-08-18** and the new rules are about *how much a change owes*, not idiom — proportionality, the sanitizer rule scoped to the change, and an escalation trigger: see the verification row above and `AGENTS.md §Verification` |
| **Find out what is queued, or whether something already shipped** | `TODO.md` §Where the work is (queue state per plugin, one table) → then the plugin's own `TODO.md`. For "did X ship?" read the `ACHIEVED.md` **ledger** — root for cross-cutting, `<plugin>/ACHIEVED.md` otherwise — and for **why** it shipped that way, take the row's SHA to `git show`, which is the only versioned copy. ⭑⭑ **The books were rewritten 2026-08-19 and the rules are `AGENTS.md §Bookkeeping`**: `TODO.md` is open work ONLY (a ✅ heading is a deletion instruction), a ledger row is ONE clause plus its SHA, and **nothing outside a queue may cite a queue** — no source comment or durable doc may point at `TODO.md §<anything>`; cite the SHA or the doc that owns the rule. ⛔ The verbose `achieved/YYYY-MM.md` shards are **deleted**; do not recreate them. A finding that shipped inside a larger chunk resolves through `ACHIEVED.md §Finding-ID map`, and a decision that produced NO commit (closed unbuilt, parked, declined) is in `ACHIEVED.md §Decisions with no commit` — nothing else in the tree records those. ⭑ **An ID prefix does not name its ledger** (`OBS-*` rows live in four): resolve any ID with `grep -rn --no-ignore-files '^| *<ID> ' --include=ACHIEVED.md .` — one ID has exactly one row, tree-wide |
| **Record shipped work / read a chunk's provenance** | Add a row to the nearest `ACHIEVED.md` ledger (chunk · date · **commit SHA** · one-clause outcome · shard). Verbose notes are optional and go in `<dir>/achieved/YYYY-MM.md`; read one with `grep -n '^## <CHUNK-ID>' <dir>/achieved/*.md` and never open a shard whole |
| **Use a CLI tool (`botmanctl`, `ircspy`, `ircspyctl`)** | `tools/AGENTS.md` |
| **Reply to a command from a thread that is not the one that dispatched it** | `include/method.h` — `method_msg_t.reply_route` (a driver-private reply address the message carries by value; `cmd_reply` prefers it over channel and sender) and `reply_sink_id` beside it (the collector divert, which still wins) — plus `core/AGENTS.md §Patterns`, the rule and the worked example. ⭑ The short version: **a route is a NAME, not a lifetime**. `method_msg_t` is deep-copied by `memcpy` at a dozen sites with no ownership protocol, so a reply address in it can be bytes and nothing else; the driver re-resolves those bytes under its own lock at the moment of use, exactly as `cmd_reply` already re-resolves `inst_name`. ⚠ A route that resolves to nothing must return FAIL, never fall back to whoever the driver is serving now — that fall-back is the misroute (`OBS-29`) |
| **Fresh-start the daemon** | `scripts/AGENTS.md` |
| Work on the factory automation (steward, digest, research/family lanes, weekend charter) | `scripts/factory/<lane>.prompt.md` (the operative instructions) + its `scripts/factory/*.service`/`*.timer` pair — **units must be re-installed to `~/.config/systemd/user/` after any edit**. ⚠ Every timer is stopped and deleted (operator 2026-07-28); FACTORY-3/4 are parked in `BACKBURNER.md` and re-authorising unattended runs is an operator decision |
| **Submit work that must not be starved by a flood of other work** (a command body — anything a person is waiting on) | `include/task.h` — `task_lane_t`, set between `task_create()` and `task_submit()` — plus `core/AGENTS.md §Patterns`. Background is the default and is right for everything the daemon does to itself; `TASK_INTERACTIVE` is `cmd.c`'s two dispatch sites and nothing else. The pool admits at most `max_threads − core.pool.reserve_interactive` (8) background tasks at once, so a `!resolve -v` flood can no longer hold every worker (`OBS-27`; measured 8.0 s → 0.1 ms). ⚠ **A blocking command body still holds an interactive worker** — the reserve bounds who may starve the surface, never how long one command may take. ⛔ And it does not touch `botmanctl`: `cmd_dispatch_as` runs the body inline on the poll thread, so that surface is serial with itself and always was |
| Add a user command (command-surface plugin) | `CMD.md` + `PLUGIN.md` §Layer Rules (placement decision-tree). ⚠ **Never write a prefix into a `usage` / `description` / `help_long` string, or into a reply that names a command** — there is no such thing as *the* prefix: `cmd_dispatch()` resolves it per bot **and per method** from `bot.<name>.<kind>.prefix`, falling back to the bot-level one and only then to `!`. Core's renderer prints what you registered and adds nothing (it printed a hard-coded `/` until 08-19; 20 core sites cleared, 55 plugin literals surveyed and deliberately left to their owners — census in `CMD.md`). ⚠ **A `cmd_arg_desc_t.maxlen` over `CMD_ARG_SZ - 1` is refused at registration** — the parser fills one `CMD_ARG_SZ` row per argument and trusts the declaration, so an over-declared one wrote into the next argument's row; where the destination field is wider than a token row, declare `0`. Rule and the nine sites that had one: `CMD.md §Argument Validation`. Cases: `tests/test_cmd_argspec.c` |
| Add or work on a misc toy / novelty command (`!math`, `!8ball`) | `plugins/misc/AGENTS.md` |
| Add or modify a plugin layering rule | `PLUGIN.md` §Layer Rules + `AGENTS.md` §Plugin Layers |
| Work on plugin lifecycle / hot-reload, or make a plugin's `deinit()` mirror its `init()` | **`PLUGIN.md §Lifecycle Contract` — authoritative, read it rather than any summary** + `core/plugin.c` (`plugin_audit`, `plugin_reclaim`, `plugin_quiesce`, `plugin_reload`) + `core/cmd.c` (`cmd_unregister_path`). Worklist: `/plugin audit <name>`; daemon-free gate: `scripts/plugin_audit.sh`. The one trap the contract cannot check for you: **a pointer another plugin handed you is invisible to every audit** — register a `plugin_unmap_notify_register()` listener or it kills the daemon later. This is the **normal condition of every service plugin with an async API**, not an edge case: quiesce and audit range-test `curl_iter_req_t.cb`, which for such a service names its own mapping and never the caller's, so the unload proceeds cleanly into the crash. ⭑⭑ **A listener alone is not the whole answer, measured 2026-08-15**: it must clear **every** pointer into the range (a streaming request carries a *chunk* callback as well as a *done* one — `SAN-22` was the second one left behind) and then **wait out the deliveries already running**, since the callback fires on the curl thread under no lock of yours — ⭑⭑ **waiting on the windows aimed at THAT mapping, never on a global in-flight count** (`OBS-33`, 08-17): a request is unlinked from the active list *before* it delivers, so the orphan count reads 0 while a consumer callback is executing, and gating the drain on that count let a measured `/plugin unload` `dlclose` the mapping out from under a live callback and kill the daemon; and because the barrier's range test names the *service's own* mapping, the service must also **end its own in-flight set from `stop()`** via `curl_request_id()` + `curl_request_cancel()` (a cancelled request still delivers, `resp.cancelled` true) or it zombies — `SC-LLM-INFLIGHT`, 4 of 8 `/plugin reload inference` refused. Worked example: `llm_stop()` / `llm_unmap_cb()` / `llm_cancel_user()` in `plugins/extension/inference/engine/llm.c`, consumed by `plugins/bot/chat/extract.c`. **Eleven** plugins now carry a listener (`inference/engine`, `whenmoon`, `reachyapi`, `searxng`, `openweather`, `weathergov`, `coinmarketcap`, `coinbase`, `gemini`, `kraken`, and `yahoofinance` since `OBS-39` — recounted live 2026-08-17); copy `reachyapi.c:reachyapi_unmap_cb` (single callback shape, mechanics commented in full) or `coinbase_exchange.c`'s `cb_fwd_*` (six callback shapes collapsed onto one tagged context). `PLUGIN.md §Lifecycle Contract` lists the three things to get right. ⭑ A leaf service whose only caller is its own `_cmd.c` in the same `.so` (`giphy`, `wordnik`, `rawg`, `tmdb`) needs none — and each of the four records why in its `*_api.h`, since the exemption ends the moment another plugin calls in. ⚠ **The other flavour fails silently instead of loudly**: a consumer that tolerates a missing provider via `plugin_dlsym_cached` must **re-resolve on every call and never latch** — core NULLs the slot on unload, so a resolve-once guard blinds the consumer for the life of the daemon (measured, PA-1 `5f4138f`: one `openweather` reload stopped the bot learning locations while the same command still answered). Rules and the worked example: `PLUGIN.md §Pointers core cannot see`. ⭑ **A plugin's reload *snapshot* is the separate question, and it now has a settled answer: persist it, or hold nothing and rebuild in `resume()`.** There is **no core stash and none is planned** (PA-10) — the clause that told plugins to "hand it to core" named a facility core never had, and it cost two chunks before anyone checked. `PLUGIN.md §Optional — suspend() / resume()` carries the ruling and the reason a blob is least trustworthy in exactly the case that motivates a reload; note the tree's only `suspend()`/`resume()` implementor (`plugins/method/reachy/reachy.c:2008`) stores **nothing**, which is the cheapest correct answer when the value need not cross the gap. ⭑⭑ **A cancel is not a barrier, and the comment claiming otherwise is where the defect is legible** (`OBS-44`, 08-18): `task_cancel` unlinks a queued task or flags a running one and **returns** (`task.h:156`), so a cancel sitting beside the free it is meant to protect closes nothing — three whenmoon comments asserted *"task_cancel runs synchronously"* while `live.c` destroyed a mutex its own periodic takes at its head. The working idiom is a per-subsystem `*_stop` that the plugin's `stop()` calls and the subsystem's own `*_destroy` still calls, for the lives where `stop()` never ran (an `init()` failure, or a plugin core never brought to RUNNING) — `wm_account_stop` / `wm_live_engine_stop` / `wm_market_persist_global_stop`. ⭑ It is safe **because there is no `stop()` → `start()` cycle**: `plugin_start_all` starts only `PLUGIN_INITIALIZED` records and `stop()` leaves `PLUGIN_STOPPING`, so nothing re-enters RUNNING without an `init()` — **ask that before moving any teardown step to an earlier phase.** ⛔ A cancel that already carries its own barrier stays put: `wm_dl_supervisor_drain` waits out a running tick on a condvar under its own lock, and moving it would put an unbounded wait into `stop()`. ⭑⭑ **`deinit()` frees; it does not destroy a lock still in use** (`OBS-34`, 08-17): core now runs the quiescence barrier **twice** — narrow (holders actually *executing*) before `deinit()`, full (anything naming the mapping) after `plugin_reclaim` — but it cannot see a holder that has not started, so **cancel in `stop()`, never in `deinit()`**. Measured: a reload landing on a live `tmdb_search_done` ran `pthread_mutex_destroy(&tmdb_cache_mu)` under it and the callback's next `trylock` returned **EINVAL**, silently, which no caller in this tree checks. ⭑⭑ **The plugin's half of that is one primitive — `curl_flight_t` (`include/curl_flight.h`, `core/curl_flight.c`), shipped by `OBS-38` 08-17.** Open a **slot** when you commit to a piece of work, `curl_flight_relay()` each leg into it, `curl_flight_close()` on the one terminal path every branch already goes through, `curl_flight_drain()` from `stop()`, `curl_flight_destroy()` from `deinit()`. ⚠⚠ **The unit is the SLOT, not the transfer** — a chained request (weathergov's point→stations→observation→forecast, openweather's geocode→One Call→hi/lo→alerts) is one piece of work wearing a new curl id per leg, so a per-transfer set empties between legs and the drain returns early; an idle slot carries a minted token, never a zero, so closing one handle cannot take another request's slot. ⚠⚠ **Close is the END of the work, not the start of the callback.** A non-zero drain means `stop()` returns **FAIL** — running and intact beats a zombie, and the flight un-grounds itself on that path. **Ten** plugins wear it since `OBS-39` (08-17): `rawg`, `tmdb`, `coinmarketcap`, `openweather`, `weathergov`, plus `kraken`, `gemini`, `coinbase` and `yahoofinance`, each of which destroyed a lock its own completions take (the three exchanges' REST freelist mutex through `*_req_release()`; yahoofinance's `yf_cache_rwl` + `yf_sess_lock`) while its `stop()` cancelled only tasks and WS readers; `claude` was in the OBS-38 row and was **not the same defect** — it destroys nothing in `deinit()` and submits no curl, its holding is a re-arming deferred task, and without a `stop()` the unload stalled the whole `unload_quiesce_ms` budget (measured 5 s). ⭑ **In a driver the flight belongs to the SUBMITTERS, not the freelist** — the slot is a `uint64_t *` parameter on `*_submit_public` / `*_submit_private`, opened just before `curl_request_create` and closed by the release funnel — because a transfer with no request context still has to ride it (kraken's WS-token mint). ⚠⚠ **A drain does not stop deliveries, it CAUSES them — into a cascade that has already unloaded your consumer**: adding one to `yahoofinance` killed the daemon on the first live gate, because `/plugin reload yahoofinance` unloads `stock` first and the cancelled chart request then called `stock`'s batch callback in freed `.text`. Check every consumer callback you hold is behind an unmap listener BEFORE you add a drain. ⭑ **Gate it with a hanging endpoint** (`nc -k -l 127.0.0.1 18099` + `set kv plugin.<x>.rest_url http://127.0.0.1:18099`): a real endpoint answers before your reload command arrives, so three earlier "gates" proved nothing — with it, 13 kraken / 18 gemini / 3 coinbase / 1 yahoofinance transfers were seen cancelled between `stopping '<x>'` and `deinitializing '<x>'`. Cases: `tests/test_curl_flight.c`. ⭑⭑ **The task-side twin is `wm_warm_chain_t` (`plugins/extension/whenmoon/warm_chain.h`), shipped by `OBS-43` 08-17, and its lesson is the SLOT lesson again: the unit is the CHAIN, not the task.** A deferred that re-arms itself replaces its own handle on every hop, so the handle you last saw is superseded by the hop you are racing and `task_cancel` on it cancels nothing that matters — arming and the *permission* to arm therefore live behind one mutex, so "cancel what is queued" and "refuse to queue more" are one decision. ⚠⚠ **Core's two barriers do not cover this and the second one is the trigger, not the net**: the pre-`deinit()` pass runs `running_only = true` and **deliberately skips a task that has not started** (`core/plugin.c:2049`, and `:2078` says why — waiting on queued work would charge the full budget to every plugin that correctly cancels in `deinit()`), while the post-`deinit()` pass waits exactly `core.plugin.unload_quiesce_ms` (**5000** live) — which for whenmoon equalled `WM_WARM_RECHECK_INTERVAL_MS` exactly, so core held the unloading thread open for precisely one recheck period and the only way a sleeping deferred leaves the timer queue is by *running*. Measured RED at `5568363` on a back-to-back `/plugin reload whenmoon`: `3 Class-B holder(s) still EXECUTING … first is task 'wm_warm_recheck' (running)`, four further re-arms **after** `deinit()` had run, and the daemon dead. ⚠⚠ **And no budget bounds the body**: one feed-only full-ring replay measured **136,861 ms** (288,000 bars, 52,544 synthesized) against core's 5,000 — so a plugin whose tasks can run long **must drain in its own `stop()` and be willing to refuse**, and the refusal must be **non-destructive**, since `stop()` returning FAIL leaves the plugin running and intact (`core/plugin.c:305`): the drain parks what it dequeued and **re-arms every parked chain** before returning FAIL. Verified live: the refusal aborted the reload, core restored all 16 dependents, and the re-armed chains promoted every market to READY. ⚠ These are predicates, not lifecycle functions — they return `true` for the good outcome, deliberately not `common.h`'s inverted `SUCCESS`. Cases: `tests/test_warm_chain.c` |
| **File a new plugin (which folder?)** | `PLUGIN.md` §Layer Rules (placement decision-tree) |
| Add a service plugin (external API) | `PLUGIN.md` + `plugins/service/AGENTS.md` |
| Wire a consumer to a service's `*_async()` API, or write one | **`include/async.h` is the contract** — every `*_async()` returns `async_rc_t`, and you free your closure on `ASYNC_FAILED_UNDELIVERED` and only that. `PLUGIN.md §Async failure semantics` tables which service refuses which way. ⚠ `ASYNC_AIRBORNE` is `0`, so a bare `!= SUCCESS` still compiles and still means "did not launch" — it just cannot tell you who owns the closure, which is the whole question. Two daemon deaths came from guessing it |
| Add/modify a **method** driver — how a bot meets humans, modality *and* wire (irc, reachy, ...) | `PLUGIN.md` + `plugins/method/AGENTS.md` + `include/method.h` |
| Add/modify the **bot** plugin — the mind that drives a bot (`chat`; there is exactly one) | `PLUGIN.md` + `plugins/bot/AGENTS.md` + `include/bot.h` |
| **Draw a table, card or aligned column anywhere** (a `!stock` table, `show attack`, a whenmoon telemetry view, a provider's trending list) | `include/display.h` — ⭑⭑ **the width is `DISPLAY_COLS` (90) and you DERIVE your total from it**, so widening one column means naming which other column pays for it; a view with a real reason to sit elsewhere says so where it defines its own total, and the renderers that predate the rule (`!stock` 103, `!crypto` 85, `show attack` 73) are **not** being swept — bring one on when you are already reworking its geometry. ⚠⚠ **`display_fit(src, cols, …)` appends its mark OUTSIDE the column budget**, so a truncated cell is `cols + 1` wide: a padded cell absorbs that in its padding and never notices, but the LAST cell of a row has no padding and lands one column past your own rule, visible only once a value is long enough to be marked (`featreq`, 08-19). Budget `cols` as the width you want MINUS the mark. Then `display_vis_len` / `display_align_right` / `display_align_left` / `display_fit` / `display_cat` / `display_rule`, implemented once in `core/display.c`, plus `core/AGENTS.md §Patterns`. ⭑⭑ **A header row and the rule under it is `cmd_reply_table_head(ctx, head)`** (`include/cmd.h`) — it READS the indent and the width off `head` itself, so a column that widens carries its rule with it and the two cannot drift. Write `head` already indented and wearing `CLR_BOLD`; do **not** hand-roll a run of `─` (three did, at three different widths, and one had drifted a column off its own data). A surface that answers over `method_send` rather than `cmd_reply` — whenmoon's `mw_send_head` is the only one — calls `display_rule` directly. ⛔ Do **not** hand-roll the width count: a colors.h marker is `\x01` + one code letter (two bytes of zero width) and a UTF-8 code point is one column, and the six renderers that each had their own copy disagreed in two measured places (OBS-8). ⚠ Pad **before** translating to ANSI, and declare the cell buffer generously — a cell too small for its own column is where the two surviving semantics differed. ⛔ Number formatting stays per-plugin on purpose (currency, precision and unit are policy, not geometry). Cases: `tests/test_display.c` |
| **Read a knob whose help says "0 = the default", or declare one** | `include/kv.h` — `kv_get_uint_or_default(key)` / `kv_get_bot_uint_or_default(name, suffix)`, plus `core/AGENTS.md §Patterns`. The schema entry is the declaration; these answer an explicit 0 with it. ⛔ Do **not** write the number a second time at the read site — 30 sites in nine plugins did, six as a `*_DEFAULT_*` `#define`, and the effect was that editing a schema default moved nothing (OBS-13, measured live). ⚠ **Opt in per call site**: a knob where 0 is a value — "no TTL", "disabled", "no cooldown" — keeps `kv_get_uint`, and a key declared 0 gets the same answer either way. ⛔ A fallback to *another key* (`identtimeout` → `maxidleauth`) is not this pattern. Cases: `tests/test_kv_default.c` |
| **Write a KV value from plugin code (not the `set kv` verb)** | `core/AGENTS.md §Patterns` — the `kv_set` bullet. ⛔⛔ **`kv_set()` does not write.** It marks the entry dirty and returns; `kv_flush()` is the sole writer and nothing outside the `set kv` verb and `irc_commands.c` calls it. So a plugin setting its own key persists only when something else flushes, at `kv_exit()`, or — since `OBS-59` (08-18) — when its owning mapping is unloaded, because `kv_reclaim_owned()` now persists its dirty entries before freeing them. ⚠⚠ **The direction that made it a defect is the CLEAR, not the set**: `kv_register` adopts the persisted row, so a clear that never landed leaves the OLDER value to win — measured as a whenmoon strategy the operator had **detached** coming back **attached** across the next `/plugin reload whenmoon`, on a market they had disarmed. ⚠ A graceful shutdown never reaches the reclaim (`main.c` orders `kv_exit()` before `plugin_exit()`, which dlcloses without `plugin_reclaim`), which is why the hole was reload-only and lasted this long — **do not reason about KV durability from restart behaviour**. ⭑ If you need a value on disk *now*, `kv_flush()` is global: it walks every bucket and issues one query per dirty entry, which is why the command surfaces call it and hot paths must not. ⛔ The nonce writes in `kraken_sign.c` / `gemini_sign.c` look like counter-examples and are not: `max(persisted, time(NULL) * 1e6)` is what keeps those monotonic, so their row is an optimisation. Cases: `tests/test_kv_reclaim.c` |
| **Declare a KV key, or work out what a `kv_register` WARN is telling you** | `core/AGENTS.md §Patterns` — the duplicate-registration bullet (`OBS-49`, 08-18). ⚠⚠ **`kv_register` and `cmd_register` warn in the same words and mean opposite things.** `cmd_register`'s duplicate silently drops the loser, so its WARN means **that command does not exist** (`AGENTS.md §Two Different "Memory" Concepts`). `kv_register`'s keeps the FIRST declaration entirely intact — value, `def`, help and callback — and discards the rest of the call, which is the **design**: a method plugin's per-bot instance KV is replayed on every rebind and releasing it would be wrong (`PLUGIN.md §Class A`). ⭑ So a replay of the same declaration is `CLAM_DEBUG`, and a `kv_register` WARN now means what `cmd_register`'s does — *what you declared did not take effect* — naming which of type / default / callback / help text differed. ⛔ Do not restore the blanket WARN (736 lines in one log, every one a report of correct behaviour, and it taught readers by analogy that a live knob was missing), and ⛔ do not make a duplicate overwrite — that lets a re-register clobber a persisted value with a compiled default, the failure `kv_load` ordering exists to prevent. ⭑ **Registering the same key with a different callback is the one case that costs behaviour**, and the census at the ship found none: 144 sites, `cb_data` NULL at every one, and the only two dynamic-key sites carrying a `cb` (`core/bot.c:886`, `llm.c:751`) pass a module-static address. The four `kv_register_owned` replay paths (`plugin.c:1159`/`2283`, `bot.c:2327`/`2376`) read `cb`/`help`/`default_val` out of a `.rodata` schema, so they are identical by construction — ⚠ and they are why an enumeration of this hazard must grep **both** spellings, not just `kv_register(` |
| **Read a `KV_STR` knob, or decide what a read-site fallback means** | `include/kv.h` at `kv_get_str` — the contract, written by `OBS-37` (08-18) as the string twin of the row above. A **registered** key never answers NULL and answers with its declaration until somebody sets a value, so `if(s == NULL || s[0] == '\0') s = "en-US";` under a schema entry that already says `"en-US"` has a **dead** first arm and a second arm that can fire only after `set kv --clear`. ⛔ Substituting the default there does not restore one — it silently refuses the clear, and writes the declaration down twice so the two can drift. ⭑ **There is no `kv_get_str_or_default`, deliberately**: 0 is typeable and ambiguous, so the integer knob had to buy its escape hatch at the read site, while `--clear` and `--delete` now say *empty* and *the shipped value* separately at the command surface (`OBS-50`). ⭑ Two dispositions, picked per site by asking whether empty is legal **downstream** — let it through (tmdb's `language=`, yahoo's `range=`/`interval=`, giphy's `lang=`, an empty KICK reason), or refuse it in the plugin's own words and name the declaration with a `#define` the schema entry shares (`OW_UNITS_DEFAULT`, `WXG_UA_DEFAULT`, `CHATBOT_PERSONALITY_DIR`, `ATK_CLASSES_DIR`). ⚠⚠ **Read the CONSUMER before picking** — openweather's two sites look like tmdb's and are not: `weather_units_wxg()` maps anything that is not `"imperial"` to metric, so an accepted empty flips weather.gov to Celsius while OpenWeather's own `units=` falls to Kelvin, silently and inconsistently. ⭑ Where a validator downstream already refuses empty **by name** (the three table-name helpers → `validate_alnum`), deleting the fallback is what makes the clear visible at all. ⭑⭑ **`OBS-48` closed this axis by re-census (08-18) and the population was nearly spent**: tree-wide there are **11** read sites substituting a *non-empty* value — the only shape this rule forbids — and `OBS-37` had already resolved all but two. ⚠⚠ **Whether the `== NULL` arm is dead is a property of the CALLER, not the key**: `kv_get_bot_str` answers NULL for a bot not bound to that plugin, so the arm is dead only where the caller has already established the binding, and a uniform sweep of them would be wrong. ⛔ The shape still worth grepping for is a **private `#define` that duplicates the schema literal instead of being shared with it** — `pricewatch.c`'s `PRICEWATCH_EXCHANGE_DEFAULT "coinbase"` against `chatbot.c:249`'s `"coinbase"`, two copies free to drift; deleted at that ship because an exchange name nothing answers to is a **documented** safe idle, so empty is legal downstream. ⭑ **The third sentinel is unreachable**: `kv_get_str` also returns `KV_REDACTED_VALUE` — non-NULL, non-empty, not the value — but secrecy is a `creds.` path segment (`core/kv.c:40`), not a name heuristic, and all **7** plain-`kv_get_str` reads of a `creds.` key already arm `kv_admin_context_set(true)` across it. Cases: `tests/test_kv_str.c` |
| **Run a SQL statement for its effect, or read one cell of a result** | `include/db.h` — `db_exec(sql, clam_ctx)` (SUCCESS/FAIL, logs the failure under the caller's own CLAM.md context), `db_result_copy` (a cell into a fixed buffer; out-of-bounds and SQL NULL are both `""`), `db_result_get_i64` (a cell as an integer, with the caller's default). Eleven copies of these three lived in six chat files before OBS-8. ⛔ The ~59 remaining inline `db_result_get` + `strtoll` sites are **not** an unfinished sweep — convert one when an edit walks past it |
| **Carry a person across an async hop inside chat** (a task data, a fetch context, a request, a queued row) | `plugins/bot/chat/identity.h` §`chat_identity_t` + `plugins/bot/chat/AGENTS.md §Concurrency`. One field, `chat_identity_take()` / `chat_identity_apply()`. ⚠⚠ Three separate carriers have shipped with fields missing and **every one of them failed silently** — an all-empty tuple resolves to dossier 0 and the reply quietly loses facts, mentions and recall (dcfc359, CHAT-NLOBSERVE-1, OBS-8/vision) |
| **Touch the URL shortener** — a short link, the redirect daemon, or the `urls` table | `web/shorturl/README.md` — it is the map of a feature that is **two processes on two hosts with one table between them**, and the table is the whole interface. `plugins/service/shorturl/` **mints** (`su_shorten` in `shorturl_api.h`: one DB round trip per link, on the CALLING thread, never under a lock) and `web/shorturl/` **resolves**, from the web server, over its own libpq connection. ⭑⭑ **`src/token.c`, `src/url.c` and `src/config.h` are compiled into BOTH targets on purpose** — the alphabet that mints a token and the alphabet that parses one must be the same code, and the `CHECK` constraints the plugin's DDL emits are generated from the same `SHORTURL_TOKEN_LEN` / `SHORTURL_TARGET_MAX` the C validates against. `tests/test_shorturl_token.c` is the gate on that pair and it covers two binaries at once. ⛔ **The table name is a `#define`, not a KV knob**, and this is the one table-owning plugin here that departs from the `plugin.<x>.table` pattern: the daemon reads the **environment** and can never see botmanager's KV, so a name only one side honoured would break resolution silently. ⚠⚠ **A short link is `<base_url>?<token>` — the token rides in the QUERY STRING and the path is nginx decoration** the daemon never consults, so `https://lame.org/p0ada` on its own is a 404 and always was. ⭑ **The daemon's DB role holds `SELECT, UPDATE` on `urls` and nothing else** — it serves a redirect and counts the hit, and it *cannot* mint; that is deliberate, because it is the internet-facing half and an INSERT there would let anyone who took it point a link anywhere. The admin CLI's `add` therefore needs the owner's credentials. ⚠⚠ **`scripts/bm-wipe.sh` destroys `urls` and there is no carve-out** — its sweep is catalog-driven precisely so no hand-kept list can go stale, and it asserts zero tables survived. `scripts/bm-save.sh save shorturl` is the ONLY thing standing between a factory reset and every published link 404ing; the wipe's pre-flight now counts them and says so. ⛔ **Shortening happens in `plugins/extension/inference/search/searxng_cmd.c`, never in the searxng service** — those same results feed `acquire_reactive.c`, which files the URL as `knowledge_chunks.source_url` and cites it into the model's prompt (`reply.c`), so shortening one layer down would fill the RAG corpus with opaque redirects that outlive whatever made them resolvable |
| Add/modify a feature plugin (weather, crypto, stock, urlgrabber, userquote, ...) | `PLUGIN.md` + `plugins/feature/AGENTS.md` + `include/bot.h` |
| Touch **weather** — the `!weather` surface or either provider behind it | The stack is **hybrid** and the routing lives in the feature: `plugins/feature/weather/weather.c` resolves a location (always through openweather's geocoders — weather.gov has none) and chains weather.gov's alerts leg into openweather's conditions leg. Presentation is provider-blind by construction: `weather_adapt.c` turns either provider's structs into the `weather_view_*` types in `weather_view.h`, and `weather_render.c` — which holds **all** presentation, including the alert line's visible-width budget — sees nothing else. Providers: `plugins/service/openweather/` (geocoding, every non-US location, and the floor under every weather.gov leg) and `plugins/service/weathergov/` — one plugin, four TUs: `weathergov.c` (lifecycle, in-flight registry, point/grid lookup + 7-day cache, and the ONLY HTTP/JSON/ISO helpers), `weathergov_alerts.c` (every active CAP alert for a coordinate in ONE GET, deduped and sorted), `weathergov_forecast.c` (the 14 day/night periods **as issued**, plus `wxg_icon_condition_id()` — all 34 icon tokens onto OpenWeather's numbering, the whole condition vocabulary) and `weathergov_obs.c` (stations → latest observation → forecast, one chain, one delivery; ⚠ **every observation value is nullable and observations are always SI**, so the service converts and each field carries a presence flag). ⭑ A caller holding better alerts passes `OPENWEATHER_ALERTS_SKIP` to `openweather_fetch_*`, and openweather's up-to-8 serial `/alert/{id}` GETs never happen. ⭑⭑ **The measured API facts now live in `plugins/feature/weather/AGENTS.md`** — the ten weather.gov frictions (the UA *is* the auth; 4-decimal coordinates; non-2xx means not-covered, and alerts answer 400 where forecasts answer 404; the 14 day/night periods; wind as a human string; SI-only observations and no Kelvin anywhere; the closed 34-token icon vocabulary), the CAP field survey off 11 simultaneous live Ohio alerts (which fields are null, and the `references[]` supersession rule), OpenWeather's empty `event` on every NWS bulletin, and the two standing decisions (`!w -h` stays on OpenWeather; the proactive alerter shipped as the soul's `weather` chore). **Re-verify, never re-derive — that evidence needed a storm.** The per-chunk record is the root `ACHIEVED.md` ledger |
| Add/modify an extension subsystem (inference, whenmoon) | `PLUGIN.md` + `plugins/extension/AGENTS.md` |
| Work on the whenmoon plugin C code (markets, candles, backtester, treasuries) | `plugins/extension/whenmoon/AGENTS.md`. ⛔⛔ **An empty strategy roster is not evidence that a market is feed-only** (`OBS-45`, 08-18): a reload cascade unloads every strategy `.so` before whenmoon's own `stop()` and each `deinit()` calls `wm_strategy_detach_self`, so `wm_strategy_loaded_iterate` **truthfully** reports zero attachments for the whole length of it. The registry cannot separate that from a genuinely advisor-less market and never will — the market's own binding `plugin.whenmoon.market.<id>.strategy` can, exactly, because a departing mapping never touches it (only the operator's `detach` verb clears it). Read it through **`wm_strategy_binding_get`** (`strategy.h`), never by formatting the key yourself. ⚠⚠ Anything that sizes, promotes or gates a market on the roster must consult the binding first: reading the zero as feed-only promoted the market to READY **and** derived `limit = 0`, which `wm_aggregator_load_history` reads as the **full 1m ring** — 288,000 bars inside `stop()`'s 40,000 ms drain, and the tree's worst single replay is **134,100 ms** (`warm_chain.h:44`), so this window is what *manufactures* the pathological case `OBS-43`'s refusal path exists to survive. Both `warmup.c` entry points now HOLD — re-arm without advancing `iters`, replay nothing, promote nothing — and log at DEBUG, because `wm_strategy_detach_self` already WARNs the operator by name (`OBS-57`) and the hold rides on that signal. ⚠ The binding survives a daemon RESTART and **not** a plugin RELOAD (`kv_set` marks dirty, only `kv_flush()` writes, nothing on the attach path calls it, and unload reclaims the entry) — filed as `OBS-59`; it does not affect the reads above, which happen while whenmoon's KV is still registered. ⛔ No unit gate exists — there is no whenmoon fixture in `tests/`; this shipped on live gates with a path-limited `git stash` control. ⭑ **Parked whenmoon work lives in `plugins/extension/whenmoon/BACKBURNER.md`** — opened 2026-08-19 with `OBS-60` (a changed product set drops and rebinds the WHOLE per-exchange WS binding, so surviving products are unsubscribed and resubscribed), moved out of the root `§SC-OBSERVED` by operator decision. Measured, unrefuted, **unauthored**, and waiting on a portfolio: read it before touching `wm_market_resub_ws`, and note the cheap subscribe-before-drop fix is **REFUTED** there (overlapping handles double-count trades, which have no id and no dedup). |
| Act as CFO / touch a treasury or the live disc fund | `plugins/extension/whenmoon/CFO.md` (read FIRST — it is the office) |
| Write, iterate, or backtest a trading strategy (cp1, mako, riptide, ...) | `plugins/extension/whenmoon/strategy/AGENTS.md`. ⛔⛔ **A strategy plugin's `deinit()` MUST begin with `wm_strategy_detach_self(<X>_NAME)`** (`OBS-26`, 08-17) — it is the one line a new strategy forgets and nothing will fail loudly if you do. `wm_strategy_finalize` is **not** called for you on an unload: whenmoon hears about a departing `.so` only through core's unmap listener, which fires *after* your `deinit()` and, per `include/plugin.h:293-295`, may not call back into the mapping that is leaving — so `deinit()` is the last legal moment and the shim is what uses it. ⭑ The fix was **a missing call, not a missing contract**: `whenmoon_strategy.h` already assigned the `ctx.user` lifetime to the strategy, and the hook simply never fired. Whenmoon `mem_free`-ing `att->ctx.user` from the listener was **measured legal** (`ctx.user` is a field of whenmoon's own struct and `mem_free` is core's allocator, not a `plugin_*` API) and **refused as the wrong trade** — do not re-propose it; it narrows a correct contract to paper over a call, and silently leaks any second allocation a future strategy makes. ⚠⚠ **The honest cost: correctness now rests on 16 per-strategy call sites**, and the backstop is one `CLAM_WARN` from `wm_strategy_unmap_cb` naming the strategy that forgot (`strategy '<x>' unloaded still holding N attachment(s)`). That WARN's meaning **inverted** — it used to explain normal behaviour, it now reports an anomaly — so a pre-08-17 log line saying the same thing is not the same event. ⚠ Measured RED at `618ac97`: kedge alone had leaked **18 blocks / 73 KB** across the session's reloads; GREEN drops exactly 3 per unload and the listener takes its quiet `INFO` branch. Bounded by **reload count, not uptime**, and the damage is **diagnostic, not capacity** — the noise poisons `mem_exit`, this tree's primary leak detector. ⚠ Unchanged and out of scope: `/plugin reload strategy_<x>` still leaves the strategy **absent from whenmoon's registry** afterwards (the row is dropped, nothing re-scans); `/plugin reload whenmoon` or `/whenmoon strategy reload <x>` puts it back. ⛔ No unit gate exists — there is no whenmoon fixture in `tests/`, and this shipped on live gates only. |
| Run or interpret a backtest / sweep (the iteration loop) | `plugins/extension/whenmoon/strategy/AGENTS.md` §iteration loop |
| Add/modify an exchange driver (coinbase, gemini, kraken, ...) | `PLUGIN.md` + `plugins/service/AGENTS.md` + `plugins/feature/exchange/AGENTS.md` + that driver's own `plugins/service/<name>/README.md` (auth + protocol specifics). ⚠ **WS handle lifetime: survive a provider reload — `exchange_api.h` §WS subscriptions.** The driver's subscriber node dies with the mapping that issued it; the abstraction owns the public handle and stamps it with a registration generation, so nothing stale reaches your `ws_unsubscribe`. Do not add a foreign-handle guard of your own, and do not hand a driver-internal pointer upward as the handle. ⭑⭑ **Every driver with a refcounted slot table owes one invariant: the gateway holds this, and nothing may forget a slot while it does** — kraken landed it as `gateway_holds` (`OBS-21`, `328ac01`), gemini as the same name (`OBS-42`). ⛔ **The two hazards are both about a name that outlives the lock hold which made it true.** (1) **Never re-derive a slot by INDEX across a dropped `mu`**: compaction is swap-with-last in every driver here, so an index taken before the send names a different slot afterwards and the completion's writes land on a live feed — clearing its belief, after which its own unsubscribe is never emitted and the gateway streams to nobody (`OBS-5`, and `OBS-42`'s D4 in gemini). Re-derive by **identity** — `(channel, symbol)` — and write only if the slot is still the one you marked. (2) **A slot with a frame already on the wire must be skipped, not emitted for again**, which needs an explicit in-flight state (`UNSUBSCRIBING`) that both a concurrent pass and compaction refuse (`OBS-20`+`OBS-35`). ⚠⚠ **And an ack can outlive the consumer that caused it**: a consumer leaving before its ack correctly emits no unsubscribe, so the ack lands on a `refcount == 0` slot that no later pass will ever look at — the ack handler is the only place that can reap it, and it must (`OBS-21` for kraken, `OBS-42` D1 for gemini). ⭑ Correlators differ and one cannot be borrowed: kraken echoes `req_id` and rings it; **Gemini MD v2 echoes none**, so identity is the whole correlator there. ⛔⛔ **And measured live 2026-08-17 (`OBS-42`'s G6, filed as `OBS-53`): Gemini MD v2 sends no `subscription_ack` AT ALL** — zero in ~7 minutes of live feed, zero in the daemon's whole log history, none to an external probe sending the driver's exact frame — so `gateway_holds` is never set there, **no unsubscribe frame is ever emitted to that venue**, and the unconditional on-open reset is the only thing that reclaims the slot. Kraken acks and unsubscribes correctly, verified beside it on one daemon. ⚠⚠ **A protocol fact read from our own parser is not a protocol fact: prove the venue sends the frame before you design against it.** ⚠ Coinbase has **none** of these — it sets the flag on send success, never drops the lock inside its emit, and has no subscribe-ack handler at all (re-measured at `OBS-42`); do not "harmonise" it into the same shape. ⭑⭑ **A ticker's `price` is the LAST TRADED price, never a book level** (`OBS-58`, 08-17) — coinbase and kraken read it off the venue's own ticker row, and **gemini is the only driver that has to synthesize one**, because MD v2 has no ticker channel. It synthesized it from the `l2` diff, taking whatever row sat last in `changes` — before the `qty == 0` test, so a *removed* level priced the tick, and on a fresh socket, where the first frame is the whole book, a resting $12.3B order did (`px=1.2345679e+10`; **348 of 6,876 ticks off by >0.5%**). ⛔ **Never publish a ticker you cannot price**: whenmoon assigns this field into `mk->last_px` unguarded and that is the mark a paper or real fill executes against, so a `0.0` erases a good price rather than meaning *unknown*. Contract at `exchange_ws_ticker_t` in `exchange_api.h`. ⭑⭑ **A subscribe answers for EVERY (channel, product) pair or for none** (`OBS-51`, 08-18): all three drivers walk the caller's cross product seating one slot per pair, and all three used to skip what they could not seat and return SUCCESS anyway — so a consumer held a live handle for a feed no frame was ever emitted for. ⛔ **Partial success is not an option here and the reason is in the CONSUMER, not the driver**: whenmoon's reconcile diffs the set it REQUESTED against the set it wants (`market.c:wm_market_resub_ws`), so a binding that is live-but-incomplete compares equal and is **never re-driven** — the dark half stays dark for the life of the binding. A driver that cannot seat everything therefore seats nothing, gives back every refcount it took, and returns FAIL, which is the one answer that reconcile reads as *retry me*. ⚠ Two of the row's own three fix shapes died on that measurement (*"report the count"* and *"FAIL only when NOTHING was allocated"*), and its scope died too: it was filed against gemini and **kraken and coinbase had the identical shape** — same 128-slot table, same allocate-or-skip, same lying return. ⚠⚠ Coinbase adds `heartbeats` + `status` to every subscribe and keys **all three by product**, so one single-product ticker subscribe seats THREE slots and the walk is in enum order — under the old code the housekeeping pairs won the last seats and the caller's own data channel was the one dropped. Cases: `tests/test_kraken_ws_channels.c`, `tests/test_gemini_ws_channels.c` (g6-g10 are the price ones; g12 is the refusal), `tests/test_coinbase_ws_channels.c` (new 08-18 — the first coinbase WS fixture in the tree) |
| **Call a function pointer another plugin registered with you** (a protocol vtable, a driver hook, any cached entry point) | The pointer is only yours while its registration is, and the registration ends on the unloading plugin's thread — not yours. Two disciplines in this tree and no third: **snapshot it under the registry's lock and call through the snapshot** (`exchange_vt_snapshot` in `plugins/feature/exchange/exchange.h`; every dispatch path in `exchange_caps.c` and `exchange_request.c` takes it that way, `OBS-24`), or **hold the lock across the call** where the callee is short and takes no lock of yours (whenmoon's strategy registry does exactly this, and `plugins/extension/whenmoon/strategy.c:10-35` says why). ⛔⛔ **What is never safe is the shape both replaced: test the pointer, then dereference it again to make the call.** Those are two reads with the unregistering write between them, and the second one calls through NULL — measured 5 SEGVs in 5 runs of `tests/test_exchange_vt_race`, with the daemon's stderr at `/dev/null` so the log just stops. ⚠ A snapshot does not cover an unload completing *while* the driver call executes; that residue is core's quiesce barrier and `plugin_unmap_notify`, and no lock of yours can reach it |
| React when an exchange provider registers or reloads (rebuild WS subscriptions / per-exchange state) | `plugins/feature/exchange/exchange_api.h` (the `exchange_watch_register` contract) + `plugins/feature/exchange/AGENTS.md §Registration watch`. The registry fires every watcher after each SUCCESSFUL registration — fresh or revived — on the registrant's lifecycle thread with no exchange lock held, so the callback may subscribe from inside the fire; whenmoon is the one consumer (`wm_exchange_watch_cb` → `wm_market_resub_ws`). ⚠⚠ **A registration is not readiness**, measured 2026-08-16 (OBS-23): a driver registers at the TOP of its `start()`, so anything the fire triggers runs before the rest of that `start()` has primed. Kraken's pair cache is the case that bit — a rebuild against an empty cache bound pass-through symbols (`ETH-USD`) that Kraken refused outright, and the feed stayed dead. A driver whose subscribe path depends on state it loads in `start()` must have that state ready **before** it calls `exchange_register` (`kr_assetpairs_prime_sync`), or its own registration will poison every consumer that reacts to it. ⭑ **The invariant now holds for two of the three drivers** — gemini got its own `gem_symbols_prime_sync` at `OBS-47` (08-17), where the register-then-load order was measured on a live reload with whenmoon's *"rebuilding ws subscriptions"* landing **between** the registration and the cache SELECT's very *submission*; coinbase is **structurally exempt** and its absence of a prime is not a missing fix, because it has no symbol translation and no snapshot table at all (its product ids `BTC-USD` already are its native form). ⚠ A prime is the DB half only: it is **one SELECT, no network and no staleness test** — the async load that follows re-judges age and refreshes. Blocking `start()` on the network fetch is the thing both drivers' async design exists to avoid. ⭑⭑ **The consumer must be told WHICH exchange, and must act only on that one** (`OBS-41`, 08-17): whenmoon logged the name and threw it away, so every registration rebuilt every venue's bindings wholesale — a `/plugin reload gemini` unsubscribed and resubscribed **Coinbase**, and the live feed the frozen kedge paper sessions run on went **~17 s with no ticks at all**, measured three times, because the resubscribe frame goes out immediately and then sits unacked until the driver's 15 s watchdog forces a reconnect. The name is now passed through as `stale_exchange` and the reconcile is a **diff**: only the registrant is rebuilt (its handles are dead — the WS generation is per-exchange, `exchange_api.h:454-466`), and a venue whose product set did not change is not touched at all. ⚠ A diff that could not tell the registrant apart would be worse than the rebuild it replaces: the newly registered provider's dead binding compares equal to itself and its feed would never come back. ⛔ It also forces a slot discipline — whenmoon hands the driver the **binding slot address** as `user`, so slots are tombstoned in place and never compacted and `n_ws_bindings` is a high-water mark; compacting under a live binding misattributes ticks between venues, silently. Verified live 2026-08-17: three registrations (gemini x2, kraken) emitted **zero** Coinbase WS frames with a max inter-tick gap of **1.0 s**. Cases: `tests/test_ws_binding.c` |
| Subscribe an exchange driver to a PRIVATE / account WebSocket channel (`EXCH_WS_USER`, fills, balances) | That driver's `*_ws.h` — and check first whether the venue serves account channels on the SAME endpoint as market data, because two of the three here do not. ⚠⚠ **Kraken v2 is two gateways and each refuses the other's channels outright** — a private subscribe on `ws.kraken.com` answers *"Private data and trading are unavailable on this endpoint"* and a public subscribe on `ws-auth.kraken.com` answers the mirror image of that sentence; a token does not change it (OBS-18, measured both directions 2026-08-17). The shape that works is one session per endpoint with the channel choosing between them — `kr_ws_session_for_channel()` in `plugins/service/kraken/kraken_ws_channels.c`, or `gem_ws_session()` in `plugins/service/gemini/gemini_ws.c` (market data + order events). Coinbase needs neither: `advanced-trade-ws` takes both on one socket, which is why a driver's shape here is a property of the venue and never a house style. ⛔ A wrong-endpoint subscribe is not loud where a consumer can see it — the handle is live and the slot merely FAILED — so the routing is pinned by a case (`tests/test_kraken_ws_channels.c` c7), not by a WARN. Rules: `plugins/service/kraken/AGENTS.md §MUST` |
| Reconcile a local subscription table against what a remote gateway actually holds (a slot says FAILED while the feed streams; an unsubscribe that is never sent) | `plugins/service/kraken/kraken_ws_channels.c` — the invariant is `gateway_holds` and the rules are `plugins/service/kraken/AGENTS.md §MUST`. ⚠⚠ **A venue's refusal string may be a statement about the VENUE, not about your request** — Kraken's `'Already subscribed'` means the subscription is live and asking again is the error; filing it as a failure clears the one flag the unsubscribe path keys off, so the gateway keeps streaming to a table that fans it to nobody, permanently (OBS-21, measured 41 times live). ⛔ Never forget a slot the remote may still hold; the deterministic cases are `tests/test_kraken_ws_channels.c` c10..c13 |
| **Decide whether a quiet stretch of a feed's history is real or just unobserved** (no trades for ten minutes — a quiet market, or an outage?) | Ask the venue rather than inferring it from elapsed time: `exchange_ws_event_t.seq_gap` (`plugins/feature/exchange/exchange_api.h`) says frames were lost on this connection, or that it reconnected. ⭑ **Coinbase only** — Advanced Trade numbers every frame it sends a connection, densely (measured over 12,440 consecutive frames in ten minutes, no skips and no duplicates), and the driver reports a skip to each subscriber on the first event that subscriber actually receives. `false` from kraken/gemini means **no answer**, not *no loss*: nobody has probed those protocols for a counter. ⛔ It answers **whether**, never **which product** — per-product tracking is exactly what `finding_cb_ws_seq_gap_false_positive` refuses, and that finding is about the LEGACY Exchange API, not Advanced Trade. ⭑ The consumer precedent is whenmoon's aggregator (`OBS-61`): over a confirmed hole it refuses to synthesize the missing minutes, drops the market out of `WM_WARM_READY` — which is where the trade engine stops acting on advice, paper and real — and re-enters the warmup lifecycle, whose convergence replay resets the rings and repairs them from the venue's own candles |
| **Decide whether a price is fresh enough to act on** (send an order, size a position, trust a cached mark) | Measure when the DATA arrived, not the stamp riding with it: `whenmoon_market_t.last_feed_ms` (`plugins/extension/whenmoon/market.h`) is stamped from our own clock in `wm_market_on_event` on every ticker and trade, and `wm_market_engine_real_submit_locked` (`live.c`) refuses a real order once it is older than `plugin.whenmoon.market.<id>.mark_max_age_ms` (60 s default, 0 disables, read fresh per submit). ⛔ **Three stamps look like the answer and none is** (`OBS-62`): a caller's `mark_ms` is a claim — the engine prefers the strategy's own `sig->ts_ms`, and a strategy is dlsym'd third-party code; the venue's event time is **absent on a kraken v2 ticker frame** (`kr_ws_dispatch_ticker` leaves `time_ms` 0), so a market gated on it could never trade; and a synthesized catch-up bar wears a **current** timestamp over a carried-forward close, which is the stale-price path itself. ⚠⚠ **Never put a wall-clock freshness test on the signal path** — `backtest.c` replays historical bars through `wm_market_engine_on_signal_with_mk`, so a bound there refuses every backtest signal; the gate belongs on the real-submit path alone. ⭐ An operator force-trade carrying an explicit price is exempt by design: that price is theirs, and gating it removes the manual exit exactly when the feed has gone quiet |
| **Recover state after being away** (a restart or `/plugin reload` re-derives a cursor, watermark or *last seen* stamp that lived in the old mapping) | Derive the floor from the state that SURVIVED, not from the clock: `wm_live_engine_start` (`plugins/extension/whenmoon/live.c`) seeds the REST fills-poll cursor from the oldest **restored** pending order's `submitted_ms`, bounded by `WM_LIVE_FILLS_MAX_CATCHUP_MS` (`OBS-64`). ⛔ A `now − <overlap>` seed is a 60-second memory of a venue you may have been away from for hours, and the paths that look like a safety net often are not: a boot reconcile that lists only OPEN orders cannot see one that FILLED while you were gone, a WebSocket has no replay, and whenmoon drops a fill it cannot match to a pending row at `CLAM_DEBUG`. ⚠ Bound the catch-up — an unbounded floor makes every subsequent poll ask for the whole gap against a page limit (100 fills here), which pushes the recent rows off the page you actually needed |
| Add/modify a DB driver | `PLUGIN.md` + `plugins/db/AGENTS.md` + `include/db.h` |
| **Write SQL from anywhere** (a query, a mutation, a multi-statement change) | `include/db.h` + `core/AGENTS.md §Patterns` — the bind-parameter and transaction bullet. A value from outside this source file goes in a `db_query_params()` placeholder, not through `db_escape()` + `'%s'`; statements that are one change go in a `db_txn_*` handle, which **pins a pool connection** until it ends and whose `db_txn_commit()` answers FAIL if any statement failed (PostgreSQL reports the COMMIT of an aborted transaction as a success). ⚠ The tree's ~258 existing `db_escape` sites are correct and are **not** being swept — the rule binds new SQL and whatever an edit already walks past |
| **Read or write one bot's setting** (`bot.<name>.<suffix>`, or the per-method `bot.<name>.<kind>.<suffix>`) | `include/kv.h` §per-bot keys + `core/AGENTS.md §Patterns` — the per-bot key bullet. Call `kv_get_bot_uint` / `kv_get_bot_str` / `kv_get_bot_method_*` / `kv_set_bot_uint` with the schema entry's **bare suffix**; do not compose the key at the call site. The grammar's write side is `bot_register_driver_kv` / `bot_register_method_kv` in `core/bot.c`, laying a plugin's `kv_inst_schema` table down over each bot — so the suffix you pass is the table's own key and the two halves cannot drift. ⚠ A composed key that overruns `KV_KEY_SZ` used to address the **shorter key it truncated onto** (`KV_KEY_SZ`'s comment records this tree paying for it once); the accessors refuse and log `kv_bot_key` instead. ⛔ Four shapes deliberately keep their own `snprintf` and are **not** a cleanup opportunity: registration (`kv_register`, 27 sites — the help text dominates), prefix builders for `kv_iterate_prefix` / `kv_delete_prefix` / a `strncmp`, a suffix composed at runtime (`voice.c`, `soul.c`'s chore table), and IRC's three-level `bot.<n>.irc.chan.<chan>.<suffix>`. `ask`/`imagine` already carry the same pattern as plugin-local helpers — leave them |
| **Hold a KV string value past the read** (`kv_get_str`, `kv_get_bot_str`, `kv_get_creds`, `kv_get_secret`) | `include/kv.h` — `kv_get_str`'s contract + `core/AGENTS.md §Patterns`, the immortal-string bullet. **Just hold it**: `KV_STR` values are interned in `core/kv.c` and live for the process, so the pointer never dangles and its bytes never change — `kv_set` installs a different string, and `kv_unregister` / a plugin unload's `kv_reclaim_owned` leaves it alone. The failure mode that remains is **staleness**, so re-read the key wherever a mid-flight change must be seen; there is nothing to copy, lock or free, and `kv_get_val_str()` is for *serializing* a value of any type, not for making a string safe. ⛔ Do not add a defensive `snprintf` copy at a new site, and do not "fix" one of the ~205 existing ones as a sweep — several carry a comment describing the old hazard, and those comments are what to delete when you are already in the file. ⚠ The property is the **string's**, not the entry's: `e->key` still dies with the entry (copy it before dropping `kv_mutex`, as `apply_val` does). ⭑ **`kv_get_help()` joined the immortals** (`OBS-14`): `kv_register` interns the help string, so the caller's storage is its own business and a plugin unload no longer leaves core reading an unmapped page. The one pointer the registry still hands out into a plugin's mapping is the `const kv_nl_t *` from `kv_get_nl()` / `kv_iterate_nl()` — a graph, not a string, so **read it and drop it**; retaining one across that plugin's unload is the crash |
| **Take a KV value back — store an EMPTY one, or retire a stored one** | `include/kv.h` — `kv_reset()` + `core/cmd_set.c`'s `set_kv_clear` / `set_kv_delete`, shipped by `OBS-50` (08-17). The operator surface is `set kv --clear <key>` (empty a `KV_STR`) and `set kv --delete <key>` (revert a registered key to its declaration and drop its row). ⛔ **The flag goes INSIDE the subcommand.** `set --clear kv <key>` was measured and refused: it needs flag handling in `core/cmd.c`'s dispatcher and a flag concept in `cmd_arg_desc_t`, the framework every plugin registers against — do not re-propose it. ⚠ `validate_kv_key` accepts `-`, so `--clear` passes validation *as a key* and reaches the handler in the key position; the flag test is positional and explicit, never a fallthrough from a failed lookup. ⭑ **`--clear` is `KV_STR`-only by measurement, not by taste** — `str_to_val` interns `""` unconditionally on that arm and every other arm fails on `end == str`, so an empty number would have to mean *zero*, which is a value and not an absence (`OBS-13` is what conflating those costs). ⚠⚠ **`--delete`'s correctness is a NEGATIVE**: `kv_reset` leaves `dirty` **false** (and clears it, since another writer may have raised it and never flushed), because `kv_flush()` persists every dirty entry and `cmd_set_kv` flushes on success — a revert routed through `apply_val` re-INSERTs the row it just dropped *inside the same command*, and nothing reports it; the operator finds the old value back after the next restart. ⛔ **`/db delete kv` is a DIFFERENT operation and neither replaces the other**: it retires an orphaned ROW and *unregisters*, and it is still the only tool for a DB-only orphan (`kv_reset` cannot see a row with no live entry); `set kv --delete` retires a registered key's stored VALUE with the entry left in place. ⛔ The four sentinel decoders this row exists to explain — giphy's rating, urlgrabber's `crawler_agent`, chat's `memory_kv_str_disabled()`, chat's `-` for `aka` — **stay standing on purpose**: giphy's `"none"` IS its declared default, all four are published in KV help text, and anyone who typed one has it persisted. ⭑ **`set bot` HAS both verbs since `OBS-54` (08-17)**, in the same position — `set bot <bot> --clear|--delete [<kind>] <key>` — and that is the tier where an empty value is most wanted, since `bot.<n>.behavior.aka` is why chat invented a `-` sentinel to spell one. It could not have them before because the three-form/four-form pick asked *"does a value follow the second token?"*, which is not a question about the form: with the value absent, `set bot karan reachy attention.mode` picked the three-arg form and addressed `bot.karan.reachy`. ⚠⚠ **The row's own prediction that this writes an *adjacent real key* is REFUTED** — measured 08-17, no bot in this tree registers a bare `bot.<n>.<kind>` key, so it failed with a *misleading* `unknown configuration key` instead; the silent-write consequence was latent in the schema, not in the code, and the sharp edge measured in its place was the other direction (a **trailing** flag is consumed as a VALUE and `kv_set` stores the literal string on a `KV_STR` key). ⛔ **A flag is recognised by POSITION on both commands and nowhere else** — leading only — because `validate_kv_key` accepts `-`, so a flag-shaped token is a legal key *and* a legal value; past that position everything is data, `--clear` included. The pick is now the token count plus `bot_has_method_kind()`, and a missing value is reported as one. ⭑ **What a read site should DO with a cleared value is the `KV_STR` row above** (`OBS-37`, 08-18) — these four decoders standing is not a licence to restate a declaration at a read site. Cases: `tests/test_kv_reset.c`, `tests/test_set_bot.c` (the form pick, a pure function — no bot, no KV, no daemon) |
| **Block a thread until an HTTP request can be queued** (backpressure for a bulk pipeline, rather than dropping work when the curl queue is full) | `include/curl.h` — `curl_request_submit_wait(req, wait_ms)` + `core/AGENTS.md §Patterns`, the bounded-wait bullet. **Pass a bound you are prepared to sit through, and decide first what happens when it fires.** Nothing else ends this wait: the submit queue drains only as the endpoints ahead of it answer, and one that accepts a connection then goes silent holds its slot for the request's whole timeout (`llm.timeout_secs` = **300 s**), so the unbounded version parked a pool worker for as long as the sickest peer chose — invisibly, because a thread in `pthread_cond_wait` logs nothing. `wait_ms` of 0 is the fast-fail submit without the queue-full WARN. The deadline is `CLOCK_MONOTONIC` (`curl_slot_cond` carries the condattr — the only condition in the tree that does). ⚠ The interesting half is the caller's: `llm_embed_submit_wait` passes `llm.embed_submit_wait_ms` (60 s) and knowledge answers a fired bound by **stopping the ingest**, because its chunk rows are INSERTed before their text reaches the batch — see `plugins/extension/inference/KNOWLEDGE.md §Concurrency`. Cases: `tests/test_curl_submit_wait.c` |
| **Run something long, blocking or repeatedly-scheduled inside a command handler** (a bulk ingest, a sweep, anything that parks) | `include/cmd.h` — `cmd_inflight_owned()` + `core/AGENTS.md §Patterns`, the running-handler bullet. **Nothing is owed at the call site: this already works.** The three dispatchers record the handler, `plugin_quiesce` counts it, and `plugin_unload_locked` refuses *before* `stop()` — so a `/plugin reload` of the plugin your handler lives in is **refused while it runs**, with the plugin left untouched and working, and the operator retries. What changed is the failure mode, not the API: before 2026-08-16 that reload proceeded and `dlclose`d the mapping the handler was executing in. ⚠ The refusal names your command, so a handler that parks for minutes makes its plugin unreloadable for minutes — bound the wait rather than lengthening it. ⛔ Do **not** wrap `plugin_hold` around a handler: that converts the refusal into a reload that blocks for the handler's whole duration, and it inverts the standing ruling (*nothing waits; the count is what holds the mapping*). ⚠ A plugin registering a `cmd_help_extender_t` would need the same accounting first — none does, and the header says so. Cases: `tests/test_cmd_inflight.c` |
| **Say why the daemon is going down** (a `/quit` reason, or make a method driver use one) | `include/sig.h` — `sig_request_shutdown(reason)` / `sig_shutdown_reason()` / `sig_reason_sanitize()` + `include/method.h`, the `disconnect` vtable slot. `/quit [reason]` takes the rest of the line and it reaches **every method driver** as `disconnect()`'s second parameter, because `method_unregister` asks `sig` for it on the way past — so a `bot stop` outside a shutdown gets the same call with an **empty** reason, which is the driver's cue to use its own wording. ⭑ **The reason is never NULL**; empty is the "none" case, so a driver needs no NULL guard. ⛔ It is sanitized **once**, at `sig_request_shutdown` — the single door — and every consumer downstream trusts the bytes: on IRC it lands in the QUIT trailing parameter, where a CR or LF would close the message and deliver the rest as a further command from an owner-level bot. Do **not** add a second check at a driver, and do not build a reason any other way than through `sig_request_shutdown`. ⭑ irc renders it (`irc_disconnect`); reachy and botmanctl are handed it and deliberately ignore it, each saying why at its own `(void)reason`. Cases: `tests/test_shutdown_reason.c` |
| **Shut down a core subsystem that owns a persist task** | `include/task.h` (`task_add_persist` / `task_persist_join`) + `core/AGENTS.md §Patterns`, the persist-thread bullet. **Keep the handle** — the subsystem's own `*_exit()` must raise a stop flag, join, and only then free what the loop reads; a join that times out refuses to unregister. Worked example: `botmanctl_exit()` + `bctl_task_cb` in `core/botmanctl.c` (`OBS-31`), regression-tested by `tests/test_botmanctl_route.c`, whose teardown order is deliberately the one `main.c` does **not** use. ⛔ `core/AGENTS.md §Init Order (main.c)` is **not** a substitute: `pool_exit()` joining every persist thread at step 4 is one caller's ordering discipline, unstated in any signature, and it hid a use-after-free that ASan reproduced 8/8 the moment a different caller tore down innermost-first |
| **Share a scalar between threads** (a live KV knob, a shutdown or dirty flag, a stats counter, a state a driver publishes) | `core/AGENTS.md §Patterns` — the cross-thread-scalar bullet. **Three disciplines and no fourth**: a knob or a flag is `_Atomic` on the DECLARATION (`sock_cfg_t`, `curl_cfg_t`, `pool_cfg_t`, `pool_stopping`, `resolve_cfg`, `method_inst_t.state`/`.connected_at`), a stats counter is relaxed `__atomic` at every CALL SITE including the readers that hold a lock (`cmd_stat_*`, `method.c`'s `msg_out`), and anything that BRANCHES to authorize a write is a **limit** — it takes the lock, and `_Atomic` there silences the report while leaving the overshoot (SAN-20). ⚠ The declaration form is the cheap one precisely because it leaves ~30 read sites untouched; do not convert a call site to `__atomic_load_n` to "match" it. ⚠⚠ **A mutex only one side takes orders nothing** — `method_set_state` writes holding none, so the readers under `method_mutex` raced it exactly as much as the unlocked ones. ⛔ `_Atomic` settles what the load IS, not what it MEANS: every one of these views stays **advisory**, and a sender may act on a state one transition old exactly as it can one instruction later. Gate rig (gitignored): `temp/obs32/rig.c` |
| Work on a core subsystem | `core/AGENTS.md` + relevant `include/*.h` |
| Add a log/event context (`clam()`) or subscribe to one | `CLAM.md` |
| **Decide what a reconnect / retry ladder should do about a cause that will not change** (absent credentials, an unconfigured endpoint, a venue refusing your key) | It does not go in the ladder — park the session in a settled state, say so once, and let the KV watch you already have re-arm it. The worked example is `plugins/service/gemini/gemini_ws.c` (`gem_ws_park_locked`, `GEM_WS_DISABLED`, `GEM_WS_MAX_AUTH_FAIL` = 3) and the rules are `plugins/service/gemini/AGENTS.md §MUST` (`OBS-56`). ⭑ **The three drivers answer this question three different ways and all three are right**: gemini parks (its Order Events session is wholly authenticated, so there is nothing else for it to do), kraken refuses at the subscribe boundary with one log line (`kraken_ws_channels.c` — its private *endpoint* needs no auth to connect, the token is per-subscribe), and coinbase **holds** the auth-only channel's slots while the public feed streams on (`cb_ws_send_delta_locked`, CB-WS-PUB-1). Copy the one whose shape you have, not the one you read last. ⚠⚠ **A ladder with no terminal state turns your loudest alarm into wallpaper** — gemini's flap WARN fired once a minute forever on *"nobody configured this"*, and an alarm whose steady state is ON is not an alarm (same shape as `OBS-26`). ⭑ Distinguish the cases with `CURLINFO_RESPONSE_CODE`: a refused upgrade is `rc=22` with 401/403, a transport failure leaves it 0 |
| Work on an exchange **WebSocket driver** — subscriptions, acks, reconnect (`coinbase_ws*.c`, `kraken_ws*.c`, `gemini_ws*.c`) | The plugin's own `AGENTS.md §MUST` is authoritative; three rules generalise across all three drivers and each was measured, not reasoned. ⭑⭑ **A venue rate-limits control frames, and refusing one looks exactly like ignoring it.** Coinbase answers an over-limit subscribe with `{"type":"error","message":"rate limit exceeded"}` instead of an ack (measured 2026-08-19, `temp/obs52/cbwsprobe.c`: twelve frames unpaced → 8 acks + 4 refusals; the same twelve at 150 ms → 12 acks), so coinbase queues them and sends one per `plugin.coinbase.ws_ctrl_gap_ms` (200; **0 disables pacing**, which is how the latch is reproduced). ⚠⚠ **Pacing defers every write the send performs, not just the bytes** — the ack stamp moves to the drain, so a frame queued just before the last consumer leaves lands its write *after* the state that authorised it is gone. A guard placed at render time was gated and measured still failing for that reason. ⭑⭑ **So arm a liveness watchdog on the state that must be true, not on a stamp**: `cb_ws_channels_sub_ack_overdue` requires a slot still reading `sent_upstream`, because reaching that timeout with nothing subscribed cannot recover — the resubscribe renders no frame and never re-stamps, leaving a predicate that is permanently true and a consumer whose only move is to drop a healthy socket (measured: one hour at one reconnect every two seconds, 1825 WARNs, ended by an unrelated plugin reload). ⭑⭑ **And only a frame carrying content may clear a flap ladder** — a handshake and a pong are both true of a session the venue has stopped answering, and resetting `backoff_ms`/`consec_fails` on either pins the interval at its base and makes the flap WARN structurally unreachable; it had never appeared once in a 136 MB log. All three drivers reset on those same two events, but only coinbase has a producer that tears down a healthy socket. ⛔ `kraken` and `gemini` have **no** subscribe-ack watchdog at all — do not assume the shape transfers. `OBS-52`, 08-18. ⭑⭑ **A fourth rule, and it decides whether a completion needs a claim: an EXCLUSIVE mark is its own ownership stamp, a SHARED one is not.** These drivers all mark slots, drop the lock to send, and write a verdict on the way back — but an unsubscribe mark is exclusive (a pass skips a slot already `UNSUBSCRIBING`, so one pass owns it) while a subscribe mark is shared (every pass marks every live slot on the channel). Match the completion on the state alone and two overlapping passes write each other's verdicts: the failing one rolls back a slot whose frame the other already sent, and the flag saying *an unsubscribe is owed* is never set for a feed the venue is streaming. Stamp the mark and apply the completion **by identity, gated on the stamp** — kraken's phase 3 (`kr_ws_reconcile_locked`, `s->req_id == items[k].rid`) and, since `OBS-46`, gemini's. ⭑ That stamp never touches the wire, so *"the venue echoes no req_id"* does not rule it out; coinbase cannot have the defect at all, because `cb_ws_ctrl_enqueue` queues instead of sending and never drops `mu`. `OBS-46`, 08-19. |
| Work on the **gemini** service plugin, or pick up a known gemini defect | `plugins/service/gemini/AGENTS.md` — the leaf carries everything the six shipped `OBS-*` rows (`46`, `51`, `53`, `55`, `56`, `67`) left behind that still governs the code. ⭑ **The queue beside it is empty and this plugin owes no code**; its ledger is `plugins/service/gemini/ACHIEVED.md`, and nothing is authored — ASK before starting anything here. ⛔ A live key is **not** permission to trade: REAL trading stays disabled (`whenmoon/CFO.md §12`) and no gemini market is seated, so **nothing in this tree subscribes to gemini** — a live WS gate needs a consumer seated first, which is a bigger ask than the WS itself. ⚠⚠ **`plugin.gemini.ws_enabled` is `false` because nothing here is testing or trading, so there is no reason to consume a stream — that is the whole reason, and it is not a freeze** (operator, 2026-08-19). Enable it for a measurement and turn it off after. Measured in such a window on 2026-08-19: **both sessions open first try and hold**, MD at ping/pong 20 s and OE answering the open with a `subscription_ack`. ⚠⚠ **That took a signer fix, and it is the fact to carry: `creds.private_key` is the HMAC key VERBATIM — its ASCII bytes.** The signer base64-DECODED it (copied from kraken, whose secret genuinely is base64); a Gemini secret is 28 characters from the base64 alphabet, so the decode **succeeded** and keyed the MAC with 21 bytes of garbage. **Every private call this plugin had ever made came back `InvalidSignature`** — surfacing as `http=400` on the OE upgrade — and nothing in the tree had exercised a private gemini endpoint to notice (`OBS-67`). ⛔ A refused *upgrade* is not necessarily an auth-config problem: read the venue's error TEXT off the REST twin before believing the status code. ⚠ `OBS-42`'s original cause was refuted at its own ship (the venue sends no `subscription_ack`); do not quote it as established. |
| Work on the inference plugin C code (LLM client, knowledge store, acquisition engine) | `plugins/extension/inference/AGENTS.md` + `plugins/extension/inference/engine/inference.h` |
| Use the LLM client (chat/embed/image/**stt**/**tts**, streaming) | `plugins/extension/inference/LLM.md` + `plugins/extension/inference/engine/inference.h`. The speech kinds are the two odd ones: `stt` sends the engine's only non-JSON body (multipart WAV to `<base>/inference`, so its service URL carries **no** `/v1`) and `tts` receives a binary one (`audio/*` or it is treated as an error) |
| Use the memory subsystem (facts, conversation log, RAG) | `plugins/bot/chat/MEMSTORE.md` + `plugins/bot/chat/memory.h` |
| Work on **what the bot remembers about a person** — how a fact is stored, corrected, named, or written at conversation speed (`/remember`, `/forget`) | `plugins/bot/chat/MEMSTORE.md` **§Merge policies** + **§The fact vocabulary** — then the code: `memory.c` (`memory_upsert_dossier_fact`), `fact_vocab.c` (the key table), `remember.c` (the verbs). Three rules carry the whole design. ⭑ **One observation resolves to affirm / replace / reject by a source ladder** — `admin_seed` > `user_stated` > `llm_extract`/`nl_observe` — higher rank replaces at any confidence, equal rank at `>=` (recency wins ties, because facts describe mutable state), lower rank never replaces a differing value but may affirm an equal one. A rejected observation leaves the row **byte-identical, `last_seen` included**, since prompt injection orders by it. The extractor is clamped to 0.95, so a stored 1.0 is always a human's. ⭑ **`observed_at` refreshes on every accepted observation and nothing else writes it** — that is the entire decay fix, and why the pre-FACT-1 drift-pile still ages out. ⭑ **Keys are canonical or they are lost**: a correction can only land on a row it can find, so `fact_vocab.c` pins key → kind → hint, the extractor's system prompt is *rendered* from it at init, and `/remember` validates against it — free-form keys stay legal everywhere else (1–3 lowercase words, underscores). Moment-state is deliberately **not** a fact (errands, meetings, right-now, anything tied to a day; a dated plan is `upcoming_event:<slug>`, owned by the deferred spine). ⚠ The zip-first weather default (`reply.c` `nl_default_for_type`) can only be exercised when NO location-shaped fact is visible in the prompt — the model bridges with explicit args when it can see one; bury the fact under `rag_top_k/2` newer rows to test it |
| Use the knowledge store (per-persona corpus RAG) | `plugins/extension/inference/KNOWLEDGE.md` + `plugins/extension/inference/engine/inference.h` |
| **Stop a corpus accumulating near-duplicate digests of the same page**, or reason about why a RAG block comes back as five paraphrases of one source | `plugins/extension/inference/KNOWLEDGE.md` **§Page-chunked corpora**. ⭑⭑ **The cause is never the ranker.** `OBS-40` (08-18) filed this as a missing collapse in `knowledge_topk_insert`; measured, the five hits' scores were all *distinct* so the strict-`>` displacement it blamed never fires, and a cosine-distance floor was refuted at **every** threshold — at 0.90 the walk reaches **rank 233** and is still the same page. The corpus was 71.6% near-duplicate at cosine ≥0.98 (93.8% at ≥0.95) because the acquisition engine re-digests the same landing pages every tick. ⭑ **`source_url` is the axis that separates a re-digest from new content**, measured: within one `section_heading`, every high-cosine pair is one page reworded and every low-cosine pair is a *different site*. So the fix is one chunk per page at ingest, not a smarter ranker. ⚠ A 10-query battery split perfectly by corpus — 7/7 acquired probes returned five copies, 3/3 archwiki probes were clean — so **archwiki never had this defect and must never be "fixed" for it**: it holds up to 43 legitimate chunks per page and the `page_chunked` flag is what keeps the supersede off it. |
| **Reply from a callback that fires after a verb returned** (a RAG query, an embed, any worker-pool answer) | `plugins/bot/chat/show_verbs.c` — `verb_reply_to_t` + `verb_reply_to_snapshot()` / `verb_reply_to_addr()`. Snapshot the method instance **by name**, and carry `method_msg_t.reply_route` alongside channel/sender: `cmd_reply` prefers the route (`OBS-29`) and a hand-rolled snapshot that omits it addresses a name the botmanctl driver cannot resolve, so **the whole reply is silently lost** while the query still runs. Measured 08-18: both async verbs in that file had it. |
| Use the acquisition engine (autonomous + reactive knowledge learning) | `plugins/extension/inference/ACQUIRE.md` + `plugins/extension/inference/engine/inference.h` |
| Work on the chat bot plugin (command dispatch + LLM conversation) | `plugins/bot/chat/AGENTS.md` + `plugins/bot/chat/CHATBOT.md` |
| Rate-limit, throttle or dedup something **per channel, per nick or per URL** anywhere in `chat` | `plugins/bot/chat/cooldown.h` + `plugins/bot/chat/AGENTS.md` §Concurrency. One slot table serves all five of them (OBS-2, `08-16`); do not hand-roll a sixth. ⭑ **It locks nothing on purpose** — the five instances sit under five different locks (a shared rwlock, three dedicated mutexes, and one shared with three *non*-cooldown siblings), so the caller takes its own and calls `cooldown_ring_peek` / `_stamp` inside it. Matching **folds case** (an IRC channel or nick is case-insensitive per RFC 2812, and three of the five used `strcmp`, so one person under two spellings of their nick held two untouched cooldowns — measured live with a control). Eviction is **LRU**, not the FIFO write cursor two of them ran under a comment claiming otherwise. The reload floor is `peek`'s last argument (TEXT-COOLDOWN-1: a ring dies with the bot handle, and an empty ring reads as *"never spoke here"*) — pass `st->created_at` for a throttle, `0` for a dedup. ⚠ Declare every key buffer `COOLDOWN_KEY_SZ`: a wider key truncates on the stamp and can then never be matched by its own peek, which is silent in both directions. ⛔ **The plugin holds TEN such tables; the other SIX must stay hand-rolled** — engagement (two-part key), CV-4 fallback (a `method_inst_t *` in the key), handoff (a nick as *payload*), and volunteer's channel (two stamps), chunk (`int64_t`) and embed (2048-float vector) rings; they share the find-or-evict shape and nothing else, and that was measured for OBS-2 — don't re-derive it and don't finish the job. Cases: `tests/test_cooldown.c` |
| **Hand async work to an engine from the `chat` plugin** (an LLM request, a curl fetch) — or drain one at teardown | `plugins/bot/chat/hold.h` + `plugins/bot/chat/AGENTS.md` §Concurrency (the `chatbot_hold_t` bullet). ⛔ **core's quiescence barrier cannot see an LLM request**: it range-tests tasks and curl requests, and the curl record under an LLM request names *inference's* callback and *inference's* heap, so a chat callback airborne inside one is invisible to both `plugin_quiesce` and `/plugin audit` — `llm_unmap_cb` NULLs the pointer and frees nothing. Every such record therefore carries a `chatbot_hold_t`, linked at **allocation** and unlinked on every free path, and its owner drains it: `owner` is a bot's `chatbot_state_t *` for per-turn work and **`NULL` for work the PLUGIN owns rather than any one bot** (the embed backfill, the live embed, both recall contexts). `chatbot_hold_shutdown(st)` runs from both `chatbot_stop()` and `chatbot_destroy()`; `chatbot_hold_shutdown(NULL)` from `memory_stop()`, **after** `memory_backfill_stop()` so nothing new can be submitted while the drain waits. The type sits in a leaf header because `chatbot.h` includes `memory.h`, so `memory*.c` can never include `chatbot.h`. ⚠ A disowned callback must still **deliver** whatever its caller awaits — a recall's `c->user` is freed only by its own callback, so swallowing the delivery strands a *different* drain for five seconds. Cases: `tests/test_hold.c` (OBS-6) |
| Work on command dispatch / the identity + auth surface (`identify`, `deauth`, `register`, `id`) | `plugins/bot/chat/dispatch.h` + `plugins/bot/chat/AGENTS.md` §The two halves |
| Turn conversation on or off for a bot | `plugins/bot/chat/AGENTS.md` §The two halves (`bot.<name>.behavior.chat.enabled`) |
| Make a bot answer to a short name (`hh:` for hedgehogg) | `plugins/bot/chat/CHATBOT.md` §Short names — `bot.<name>.aka`, comma-separated, `-` empties it. ⭑ The rule that keeps it safe: a short name addresses the bot at the head of a line, and elsewhere **only when marked** (`@hh`, `hey hh,`), because the bare mid-sentence scan the real nick gets would make a two-letter name answer prose. Per-bot, not per-method; the robot's spoken wake phrases (`bot.<name>.reachy.attention.names`) are a separate mechanism |
| Work on interpreted command output (a persona retelling a tool's reading) | `plugins/bot/chat/CHATBOT.md` §Interpreted command output + `plugins/bot/chat/interpret.c` (capture/settle/cue) + `core/cmd.c` (`cmd_sink_*`, the reply-sink registry the capture rides on). ⭑ **The register of the ask decides**: a banged command gets the renderer's block, prose gets the persona's voice, unconditionally — CARE-4 retired the per-persona `interpret:` opt-in and `METHOD_CAP_SPOKEN`'s force clause with it, so there is no selection logic left to configure |
| Work on **deferred work** — `/in` (run a command later), `/remind`, `/show deferred`, or any chore that needs a durable row | `plugins/bot/chat/CHATBOT.md` §The soul → *The deferred spine* + `plugins/bot/chat/deferred.c`. One table (`chat_deferred`) holds every durable thing the bot owes a human, kinds `say` (speech held until its moment) and `run` (an action held until its moment, dispatched under the asker's stored identity). ⭑ **Permission is checked twice** — `cmd_permits` at schedule time AND again at fire time against the stored tuple, refusal spoken, never silent (the acts-vs-observations rule). Only **NL-capable root** commands are schedulable, uniformly on both invocation paths — that is what keeps `/in` a user feature instead of a way to schedule lifecycle verbs. ⚠ A claim whose method is gone is un-claimed so it delivers late, **except** a repeating row, whose successor the claim statement already wrote — un-claiming that one writes a new successor every tick (measured). ⭑ **A row's moment is a clock or a person**: `due_at` for the first, `deliver_on_presence` for the second (CARE-2 — `soul_on_seen` claims it on a sighting; the venue is the ROW's, so a DM wish stays a DM). The two claim paths are disjoint and share one deliver. The user-facing form is the literal duration **`back`** on either verb (CARE-2b), which resolves to a presence row held disarmed for `back_quiet_secs` first — you are present when you ask. ⚠⚠ **`cmd_nl_t.when` is the ROUTING field**: a `back` clause added there misrouted canonical `/remind` utterances onto `/in` (measured); form goes in `.syntax` + examples only. `expires_at` is written by chores with a deadline (CARE-5: two days) and defaulted to 30 days on a presence row; `repeat_secs` is read by the fire path and written by nobody yet. ⭑ **The row's `source` decides its voice class** (CARE-5): `remind`/`in` are `SOUL_CLASS_TIMED` and can never be refused — the call only records them — while a chore-written row is `UNSOLICITED` and really is gated. Quiet hours are answered *before* the claim, in the presence task; a budget refusal after the claim **returns the row to pending** rather than eating it, because unlike a weather cue a row has somewhere to go back to and its own `expires_at` already says how late is too late. One insert door for everyone: `chatbot_deferred_insert()` |
| Work on the soul (heartbeat, presence, the weather watch, occasions, follow-ups, the price watch, a new chore) | `plugins/bot/chat/CHATBOT.md` §The soul, **§Price watch** (CARE-7 — the only chore whose moment is neither a clock nor a person, and the one that reaches OUT of the plugin: one bulk-ticker snapshot per sweep through `feature/exchange`, admin-curated `chat_watchlist` per bot, one-shot `chat_pricewatch` thresholds per user, and `source='pricewatch'` classed **ASKED** so quiet hours delay the report without the budget ever silencing it. ⚠⚠ Two traps it paid for: the exchange is the "FAIL means the callback ALREADY fired" convention — `PLUGIN.md §Async failure semantics` — so freeing the sweep on the FAIL return is a double free that kills the daemon with no FATAL line; and the NL COMMANDS prompt block is **sorted by name and truncates at the tail**, so adding a command can silently delete a later one from the model's vocabulary — the builder now WARNs, and `CHATBOT_NL_COMMANDS_MAX_BYTES` is the number to raise), **§Occasions** (CARE-5 — the template every fact-driven chore copies: scan a steered fact family → `soul_claim_take()` once per subject-and-occasion in the shared `chat_soul_claims` ledger → write a presence row and let the spine deliver it. The chore itself speaks nothing, which is what keeps venue, restraint and exactly-once out of it) and **§Follow-ups** (CARE-6 — the same template's second instance, and the proof it generalizes: a second date family, no new table, no new throttle, and `source='followup'` classing it UNSOLICITED by construction. Its one addition is calendar arithmetic — `YYYY-MM-DD` or `YYYY-MM`, month-only resolving to the month's LAST day, compared at local **noon** so daylight saving cannot walk a date across the window's edge, and a day the calendar does not have skipped rather than rolled forward), **§The voice budget and quiet hours** (CARE-3 — the ONE throttle every chore calls before it speaks: `soul_voice_permits` in `plugins/bot/chat/voice.c`, classed by *who chose the moment*; a chore that claims durable work must ask `soul_voice_window_open` FIRST, because quiet hours mean later and a spent budget means never. No chore may grow a throttle of its own — weather's `max_per_hour` was retired into this one) **and §The initiative rule** — the latter is binding doctrine on what a chore may speak about unprompted (explicit ask or stated fact only; nothing derived), plus the capability/authorization/disposition split and the acts-vs-observations rule; cite it, don't restate it — + `plugins/bot/chat/soul.c`. ⚠ Every chore knob must join chatbot.c's KV descriptor table — a free-form `bot.*` key refuses `set kv` |
| Score a persona / run the SOUL battery rig | `scripts/soul/` (`spies.sh` holds the IRC presences in a long-lived shell; a round is `reset.sh <persona> && probe.sh <persona> <rN>`; dim-6 halves `remind_probe.sh` / `watch_probe.sh`, output round via `SOUL_ROUND`) + root `TODO.md §SOUL-PLAN` (rubric + scoreboard) |
| **Measure or change semantic recall / retrieval ranking** | `scripts/recall/` — the offline replay rig: `extract.sh <dir>` → `replay.py <dir>` → `judge.py <dir> <arm>`. ⭑⭑ **Use it before the A/B rig, not after.** The whole reply path is reproducible from the DB plus the embed endpoint, so a retrieval question needs **no live bot, no operator and no probe lines** — which is the trap `plugins/bot/chat/TODO.md §CHAT-AB-RIG` exists to warn about (probes pollute the speaker's corpus and outrank real answers within two probes). `replay.py` mirrors `memory_recall_scan_convo()` exactly — self-exclusion, float32 cosine, over-fetch ×3, normalized collapse, `recall_top_k`; change one and it stops measuring production. `judge.py` scores an arm against production **blind and both ways round**, counting only self-consistent verdicts, because position bias is large enough to invent findings. The measured record is `plugins/bot/chat/TODO.md §EMBED-TRUTH` (9 sections) and the code is `plugins/bot/chat/memory_rag.c`. ⚠ The rig was rebuilt from scratch twice before it was kept — it is here so there is not a third time |
| Add/modify conversational admin commands (`/dossier`, `/bot … hush`, `/show bot …`) | `plugins/bot/chat/AGENTS.md` + `plugins/bot/chat/CHATBOT.md` §Admin commands |
| Stand up a new chat bot instance | `plugins/bot/chat/CHATBOT.md` §Creating + `scripts/AGENTS.md` §bm-restore.sh |
| **Find out whether a wipe stranded rows**, or key new plugin state to a user namespace | `db orphans userns` (`core/cmd_db.c`) — reports every row whose `ns_id` no longer resolves, over every table the catalog says has that column, so a new plugin table needs no registration anywhere. ⛔⛔ **A foreign key to `userns` does NOT survive a wipe**: `DROP TABLE userns CASCADE` removes the constraints pointing at it and leaves the referencing tables, after which `CREATE TABLE IF NOT EXISTS` never restores them — measured 08-20, four chat tables declare the reference and carry no constraint, and 13 of 17 `ns_id` tables have none at all. Run it after any wipe+restore; the why and the collision traps a repair hits are `scripts/AGENTS.md §bm-wipe.sh` |
| Add/modify an output contract | `personalities/AGENTS.md` §contracts + **§The honesty floor and the disposition rule** — every contract that does not already demand a plain "yes, I'm a bot" carries the floor verbatim, and its wording is load-bearing (it was rewritten twice against live persona failures; the section says which sentences and why) |
| Edit a persona's "never claim to be an AI" clause | `personalities/AGENTS.md §The honesty floor and the disposition rule` first. A persona sets *disposition* and may never widen what the mechanism allows: phrase the clause as a deflection, never as an instruction to deny, because the contract floor outranks it and a persona that instructs a flat denial simply gets overruled mid-sentence. Four stock personas (`hands`, `karan`, `pacmanpundit`, `dalebaumhauer`) were reconciled with the floor on 2026-08-11 — match their shape |
| Make a bot's output **bold or coloured** (`!ask` answers, chat replies, any future emitter) | `personalities/AGENTS.md` §Bold and colour (the vocabulary + what a contract should say about it) + `core/colors.c` `color_markup_translate()` — the shared `**bold**` / `<red>…</red>` → `colors.h` marker rewrite, applied by `ask_cmd.c`, `reply.c:send_line_marked` and `claude.c:claude_reply_multi`. ⚠ It is **unconditional** on the chat path: a contract that never mentions markup still gets the model's unprompted Markdown bold rendered instead of leaked. `prompts/ask_default.txt` is the reference wording; `prompts/claude_builtin.txt` is the same contract restated for a dev-facing one-shot bridge |
| Work on the `/claude` bridge / daemon-restart flow | `plugins/extension/inference/claude/AGENTS.md` + `scripts/botman-restart.sh` |
| Work on `!imagine` (text-to-image) or the `image` LLM kind | `plugins/extension/inference/imagine/` (the only backend) + `plugins/extension/inference/LLM.md` §Image generation. The command owns a FIFO with per-caller quotas (`plugin.imagine.max_inflight` / `max_queue` / `max_per_user` / `max_per_anonymous`) and hosts the bytes itself into `output_dir`, served as `public_base`. ⚠ **The engine's reported MIME is always `image/png` and Gemini returns JPEG** — sniff the payload, never the header. The out-of-tree `zimage-service/` that used to back this was retired 2026-08-05 when the GPU went to the robot; its 13 gitignored docs remain on disk and describe a service nobody runs |
| Work on a **leaf service with a command surface** (`!giphy`, `!rawg`, `!tmdb`, `!wotd`/`!dict`, and every future one) | `plugins/service/<name>/` — one plugin, always the same three files: `<name>.c` (provider) + `<name>_cmd.c` (command surface) + `<name>_api.h` (contract). The pattern and when a service may carry commands at all: `plugins/service/AGENTS.md` §Leaf services with a command surface + `PLUGIN.md §Layer Rules` Rule 1 |
| Work on stock/equity/fund/commodity quotes (`!stock`) | `include/stockquote.h` (contract) + `plugins/service/yahoofinance/` (provider) + `plugins/feature/stock/` (command). ⚠ The Yahoo v7 enrichment tier is built but **shipped switched off** — `getcrumb` 429s this public IP; the wait-then-re-test procedure is `TODO.md §STOCK-4-RECHECK` and it is the only open item there |
| Work on per-userns symbol lists (`!stock`/`!crypto` `--list`/`--add`/`--del`, `@name`) | `plugins/feature/stock/stock_lists.c` + `plugins/feature/crypto/crypto_lists.c` — a list is **typed** (one table per class, no `class` column); do not build a shared lists service, and do not re-litigate it (the rejected alternative — a shared lists service behind a dlsym shim — is argued out in `8994c33`'s commit body) |
| Work on `!note` (leave a message for an absent user, delivered when they're next seen) | `plugins/feature/note/AGENTS.md` |
| **Take the next item off the feature-request board** (the operator invoked `/featreq`, or asked you to work the queue) | `.claude/skills/featreq/SKILL.md` — **invoke the skill, do not improvise the workflow**. One row per run, oldest `new` first, `in-prog` before the first edit and `completed` only once verified. ⚠ The oldest item is `show feature --status new --sort old`, **never the newest-first list read backwards**: the row cap trims the far end of the sort, so backwards yields the oldest of the newest N and not the oldest at all |
| Work on `!feature` / `show feature` (the feature-request + bug board) | `plugins/feature/featreq/AGENTS.md`. ⭑ The table is **GLOBAL** — no `ns_id`, unlike `note` and `userquote` — and one `featreq_vocab.c` owns both closed word lists, so the DDL defaults, both parsers, the colours, the "one of: …" errors and the `--sort status` `CASE` all read the same arrays. ⚠ `feature status` and `feature note` are real subcommands, so `resolve_subcmd_locked()` eats their words: a description beginning with either routes to the owner-gated setter and a non-owner is denied rather than filing. That applies to every command with children — do not "fix" it with a permission check inside the parent handler. ⭑ `feature note <id> <text>` is where an investigation row's ANSWER lives — one note, owner-only, rendered on the card under the description and nowhere on the table |
| **Rebuild the Reachy voice stack from scratch** (dead SD card, replaced robot, rebuilt 4090 host, or "what would it take to do this again?") | **`plugins/method/reachy/BRINGUP.md`** — the runbook, in order: fresh image → SSH key → the mic-ribbon trap → earbridge on the robot → whisper/kokorod on the host → LLM registry rows → plugins → bind a bot. It also lists what git does *not* carry (model weights, installed units, DB rows, the `scripts/` helpers). Keep it current in the same commit as any change it describes |
| **Work on the Reachy Mini robot** (body, senses, voice embodiment) | `plugins/method/reachy/TODO.md` — read its §C charter FIRST (measured hardware facts, ratified decisions, the do-not-re-attempt list, and the operator install gates), then the chunk you are executing. Code today: `plugins/service/reachyapi/` (REST mechanism + `reachyapi_api.h`), `plugins/feature/reachycmd/` (**the robot's verbs, scoped to the bot that owns the robot**: `/bot <name> do|**say**|wake|sleep|volume|track|wobble` and `/show bot <name> robot|moves`, offered only to a bot with the `reachy` method bound (`kind_filter`) and **admin-gated by their parent**. `volume`/`track`/`wobble` write `bot.<name>.reachy.*` **before** touching the robot — the KV is the control surface and the robot is a cache of it, re-synchronised on every connect. reachycmd owns **no plugin KV at all**, by construction: a plugin-level row may hold only what is true of every reachy. `say` is synthesize→upload→play over the engine's `tts` kind, voiced by `bot.<name>.reachy.tts_model|tts_voice|tts_speed`; ⚠ **the daemon is ASYMMETRIC about torque** — `goto_sleep` disables the motors itself at the end of its move and `wake_up` never enables them, so `wake` is two hops and `sleep` is one, and a move sent to a limp robot is accepted, returns a uuid, completes and moves NOTHING with no error on any surface. `/show bot <name> robot` reports the mode), `plugins/method/reachy/` (**the method driver** — `/bot addmethod <bot> reachy` gives a chat bot ears: an `reachy_ears_<bot>` persist thread long-polls earbridge, hands each utterance to the engine's `stt` kind, gates it on `bot.<b>.reachy.attention.mode` and `method_deliver`s it into the ordinary chat pipeline; `connect()` stands the robot up only when it finds it down, and `suspend()`/`resume()` exist for one reason — core calls `suspend()` only on the reload path, which is the ONLY way this driver can tell a reload from a `bot stop` and so the only way a reload avoids playing `goto_sleep` over its own wake; and **a mouth**: `send()` queues the reply line into a 16-slot per-bot ring and a `reachy_mouth_<bot>` persist thread synthesizes it through the engine's `tts` kind, uploads it, plays it and paces itself off the WAV's own byte rate so lines never overlap — ⚠ **`barge_in` defaults OFF** because the array's `speech_detected` flag is true in half of a *silent* room, see `ACHIEVED.md §BARGE-TRUTH`), and `senses/` — the out-of-daemon speech hosts, **both live**: `senses/whisper/` (unit + verifying model fetcher for the packaged whisper.cpp STT server on :8004) , `senses/kokorod/` (**ours**, a standalone C Kokoro TTS daemon on :8003 — `POST /v1/audio/speech` → 24 kHz **stereo** WAV peak-normalised to −3 dBFS; both of those output properties are mandatory, see its ledger). Nothing under `senses/` is a plugin or links core; kokorod is meson-guarded on `dependency('sherpa-onnx')` so a tree without the package still builds. Model weights are gitignored. Third host: `senses/earbridge/` — **ours, C, and it runs ON the robot's CM4**, not here (plain Makefile + `deploy.sh`, built remotely; raw mic PCM never leaves the robot over REST). It serves utterances at `http://reachy…:8090/utt?after=&wait=` as 16 kHz mono WAV with `X-Utt-*` headers, and gates each one on the loudest 250 ms window (`--floor-db`, default −35 dBFS) because the XVF3800's speech flag fires on the CM4's own cooling fan and whisper transcribes fan noise into fluent English — **tune that flag by watching `quiet_discards` against `utterances` in `/health`; it is a knob, not a constant, because the fan drifts**. **When the question is "what is the robot actually hearing?", run `scripts/reachy_mic_probe.sh`** — it records raw, VAD-ungated audio off the same shared PCM and hands back a playable WAV plus a level timeline, which is the only way to tell a sensor problem from a transcription problem. ⭑ **An async reply DOES reach `botmanctl` now** (OBS-29): the reply address travels in the message (`method_msg_t.reply_route`), so a one-shot needs only `botmanctl -w <ms>` to stay and hear it — the IRC dance is no longer required for this reason. `tools/ircspy` + `tools/ircspyctl` in `#botman` remain the way to verify what a *bot* says; botmanctl still proves the *action* (watch `GET /api/move/running` on the robot) |
| Work on the attack duelling game (`!attack`, `!heal`, `!defer`, `show attack`; character sheets in `plugins/feature/attack/characters/`) | `plugins/feature/attack/AGENTS.md` |
| Work on the one-shot LLM command (`!ask`) | `plugins/extension/inference/ask/` + `plugins/extension/inference/LLM.md` |
| Work on the search commands (`!search`, `!image`, `!news`, `!video`, `!music`) | `plugins/extension/inference/search/searxng_cmd.c` (command surface) + `plugins/service/searxng/searxng_api.h` (mechanism). ⭑ A search returning **exactly one** result offers that URL to the title-grabber via the `url_offer` clam event (`CLAM.md §Cross-plugin contracts`) — a multi-result search offers nothing, since a title per line would bury the results. It is an event and not a call because an extension may not depend on a feature |
| Merge a command surface into its leaf service | `plugins/service/AGENTS.md` §Leaf services with a command surface + `PLUGIN.md §Layer Rules` Rule 1 |
| Work with the IRC method driver or its identity projection | `plugins/method/AGENTS.md` + `plugins/method/irc/AGENTS.md` + `plugins/method/irc/irc_identity.h`. ⭐ **The wire has a tested translation unit at each end and they are the two files to read first.** Inbound is `irc_parse.c` (`tests/test_irc_parse.c`): what it fills is what `bot_identity_resolve()` reads, so it **refuses** a line whose prefix, nick, user or host did not fit rather than shortening one. Outbound is `irc_protocol.c` (`tests/test_irc_protocol.c`) — the formatters, and ⚠⚠ **the plugin's one `sock_send`, which is therefore the trust boundary**: `irc_send_raw` folds CR and LF out of the formatted line, because a break inside a `%s` argument ends that command and the server runs the rest as a further one from a bot holding OPER. The fold is deliberately **narrower than `sig_reason_sanitize`'s** — colour, bold, reset and CTCP bytes are put in on purpose and must survive — and a second scrub at a caller is the wrong shape. The leaf `AGENTS.md` carries the rule |
| Ingest or refresh a knowledge corpus | `plugins/extension/inference/KNOWLEDGE.md` (**§Idempotency**) + `scripts/AGENTS.md` §fetch_archwiki.sh. ⭑⭑ **Re-running `/knowledge ingest` over the same path is a RESUME, not a duplicate** (`OBS-16`, 08-17): new chunks are inserted, chunks stored without a vector are re-embedded, chunks already complete are skipped — keyed on `(corpus, source_url, section_heading, md5(text))` via the UNIQUE index `knowledge_chunks_dedup`. ⛔ **The hash is `md5` because `sha256` cannot be indexed over `text`**: it needs a `bytea`, `convert_to()` is only STABLE so an index may not call it (`functions in index expression must be marked IMMUTABLE`), and `text::bytea` is worse than useless — it reinterprets, so `'AB'` and `'\x4142'` hash **equal**, and text opening `\x` plus a non-hex byte raises `invalid hexadecimal digit` and fails the INSERT (arch wiki code blocks contain exactly that). ⛔ **And the key is FOUR columns, not the three that were proposed** — two archwiki sections legitimately carry the same body, and dropping `section_heading` deletes one of each such pair while catching nothing extra. ⚠⚠ **It is additive-idempotent, NOT a mirror**: content deleted or edited upstream leaves its old rows behind, and changing `knowledge.chunk_max_chars` re-chunks the corpus into text that collides with nothing and inserts a full second copy. ⚠ `scripts/fetch_archwiki.sh` resumes only with its default `BASE_URL`; `BASE_URL=""` puts the mktemp scratch path in `source_url` and turns the resume off. ⛔ A `/knowledge reembed` sweep was **refused, not deferred** — the orphan population is zero and the insert repairs a vector-less row for free whenever its text is presented again; the residue is that an *acquired* corpus's orphan can never be repaired that way, because its text came off the web and no re-run presents it again. Cases: `tests/test_knowledge_dedup.c` |
| Work on the dossier subsystem / `/dossier` admin commands | `plugins/bot/chat/DOSSIER.md` + `plugins/bot/chat/dossier.h` |
| Work on LLM fact extraction (dossier_facts) | `plugins/bot/chat/FACT_EXTRACT.md` + `plugins/bot/chat/extract.h` |
| Edit or add a chat personality | `personalities/AGENTS.md` + `plugins/bot/chat/CHATBOT.md` |
| Build system changes | `BUILD.md` + root `meson.build` + relevant subdir `meson.build` |
| **Run the tests, or add a case to them** | `tests/meson.build` + the suite you need — `tests/test_util.c` (`util_redact_url`, `util_url_is_safe_https`, `util_b64_*`), `tests/test_colors.c` (`color_markup_translate`), `tests/test_json.c` (`json_unescape` — the LLM stream's escape decoder), `tests/test_irc_parse.c` (`irc_parse_line` — the fields an identity is resolved from), `tests/test_irc_protocol.c` (`irc_send_raw` — the bytes that reach `sock_send`: on IRC the line ending IS the message boundary, so a CR or LF that survived the format runs the rest of the string as a further command from a bot holding OPER. The rows pin the fold *and* pin its narrowness — colour, bold, reset and CTCP bytes are deliberate and must come through. Stubs the four symbols that TU reaches outside libc, so no core and no socket), `tests/test_fact_merge.c` (the FACT-1 merge ladder), `tests/test_db_txn.c` (`db_query_params` + the `db_txn_*` trio, driven through a driver that runs no SQL and only records — **no database needed**, unlike its `fact_merge` neighbour), `tests/test_kv_bot.c` (the per-bot key accessors — that the driver and method grammars stay distinct, and that a key too long to compose refuses instead of answering as the shorter key it truncates onto; `kv_init()` is DB-free and nothing here flushes), `tests/test_kv_str.c` (the lifetime `kv_get_str` promises its ~205 callers — that a `kv_set` does not rewrite a pointer already handed out, and that dropping the entry does not take the string with it. ⭑ Both rows are stated as **pointer identity**, not as a crash, so the suite goes red on a regression under the ordinary build; the use-after-free half additionally shows as `heap-use-after-free` under `-Db_sanitize=address`), `tests/test_curl_submit_wait.c` (the bound `curl_request_submit_wait` promises — stated as **elapsed milliseconds** around the call, so the suite is red under the ordinary build. Its fixture stalls the queue with `core.curl.max_active = 0`, which means the drain promotes nothing and **no case touches the network**; against the unbounded version the full-queue row never returns and meson reports a timeout)., `tests/test_curl_flight.c` (what a plugin's `stop()` proves: that when `curl_flight_drain` returns zero, no completion callback of that plugin's is running or ever will be. Both failure directions are silent — a drain that returns early lets `deinit()` tear down under a live callback, one that never returns turns an unload into a hang — so the rows are **elapsed milliseconds** and a slot count. Same fixture as its neighbour, `core.curl.max_active = 0`, so **no case touches the network**: the drain's own cancel is what delivers them). Run: `ninja -C build && cd build && meson test`. ⭑ **The suite is deliberately tiny and stays that way** — root `TODO.md` axes A27/A28/A31 filtered it to **silent-wrongness surfaces only** (a secret in a log, a fetch of a private address, forged formatting, a person's fact quietly overwritten). Loud, recoverable, plugin-local failures are untested **by design**; do not "improve" coverage by adding cases for them. Each surface is one table under one loop. ⚠⚠ **`test_fact_merge` needs Postgres** because the ladder *is* SQL: it renders `MEM_FACT_OBSERVE_SQL` from `plugins/bot/chat/memory.h` — the same template `memory.c` sends, which is why the macro lives in the header — against a **TEMP `dossier_facts` that shadows the real table**, with `search_path = pg_temp` and a `ROLLBACK`. It reads `botman.conf` itself and **SKIPs (exit 77)** when the DB is unreachable, so a green run is not proof it ran; check the log for `52 checks`. ⭑ A new case must be shown to **fail** before it is trusted — mutate the thing under test, watch that one row go red, revert |
| Resolve a hostname from core, or change what `core.resolve.timeout` bounds | `include/resolve.h` (the contract, and the note above `resolve_lookup`) + `core/resolve.c`. ⛔⛔ **The resolver is the POSIX one on a worker thread and stays that way — operator ruling 2026-08-16.** `resolve_lookup` puts a `TASK_THREAD` task on the pool and the worker blocks in `getaddrinfo` (A/AAAA) or `res_nquery` (everything else); **do not propose per-call resolver tuning, a private resolver, or routing A/AAAA through `res_nsearch`** (which would drop `/etc/hosts`, `myhostname`, `mymachines`, the systemd-resolved cache and DNSSEC). It follows that **`core.resolve.timeout` (default 30 s) is patience, not a bound**: neither call takes a bound from us, so it decides how a *failure* reads and when a late answer is logged — **never whether an answer is kept** (`OBS-11`: it used to free a successful lookup's records and report `ETIMEDOUT` because the *queue* was slow). ⭑ **One production caller**: `core/sock.c` (`RESOLVE_A`, every TCP/UDP `sock_connect`); the only other is `!resolve`. A *session's* bound is `core.sock.connect_timeout`, enforced by the connect-watchdog sweep — which also means a late DNS answer can arrive after the watchdog closed the session, so `sock_resolve_done` guards on `SOCK_STATE_RESOLVING`. ⚠⚠ **DNS timing is unmeasurable on this network**: `10.68.254.254` (house firewall, dnsmasq, only nameserver) **NATs all port-53 traffic back to itself**, so `203.0.113.1` answers in 0.000 s from container and host alike — a probe that beats a black hole has measured the NAT. ⚠ On glibc 2.44 writing `nsaddr_list[0]` after `res_ninit` does not repoint a query; use `resolv.conf` |
| Understand overall architecture | `DESIGN.md` |
| Find a header's purpose | `include/AGENTS.md` |
