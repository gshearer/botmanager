## TASKROUTER.md — Go to your task and minimize input token spend

This is the project's **single routing index**: each task maps to the
smallest set of docs that lets you execute it. When you add or move a
module, folder, feature, or doc, update the relevant row here **in the
same commit** (per `AGENTS.md §MUST`). Keep rows task-shaped ("I want
to X") and point at the per-directory `AGENTS.md` leaf rather than
listing every file — the leaf carries the detail and is maintained
alongside its code.

| Your Task | Read These Files |
|-----------|------------------|
| **Get oriented in a fresh context (project briefing)** | `PRIMER.md` |
| **Write, refactor or review ANY C in this tree** | The **`posixc-pro` skill** (`.claude/skills/posixc-pro/SKILL.md`) — load it *before* opening a `.c`/`.h`. It is the authority on idiom, on the banned-functions table and on the modern-interface defaults, and `AGENTS.md §MUST` cites it rather than restating it. ⚠ It is **its own git repo** and does not travel with a botmanager clone, so the two rules that bite most often here are written out: ⭑ **a bounded string copy is `strlcpy(dst, src, sizeof dst)`** — `snprintf` formats and does not copy (it is *not* banned, so the tree's ~968 `snprintf(dst, sizeof dst, "%s", src)` pure-copies are a mismatch with the preferred form, not a defect: convert one when you are already editing it, never as a sweep), while **`strncpy` IS banned** as a fixed-width *field* function — no NUL on truncation, pads the whole buffer — legal only where you want that padding and tagged `// posixc-pro: strncpy — <why>` when you do. ⚠⚠ Converting one is where the mistake lives: `strncpy(d, s, SZ - 1)` becomes `strlcpy(d, s, SZ)` and **the `- 1` must be dropped**, or the buffer silently truncates a byte early forever, with no warning and no test that would catch it. ⭑ **Never add a check that cannot fail** — the same rule PA-5 deleted 171 branches for (`include/alloc.h`), and `strlcpy`'s truncation return is only worth testing where truncation actually matters. ⚠ `strlcpy` needs a `-std=gnu*` build: glibc gates it behind `__USE_MISC`, so under any `-std=c*` it vanishes and gcc's implicit-declaration error helpfully suggests `strncpy`, which is how the old idiom reasserts itself. Measured 2026-08-13, glibc 2.44 / gcc 16.2.1. ⭑⭑ **The build is HARDENED since `SC-HARDEN` (08-15)** and one flag changes how you write C here: `-D_FORTIFY_SOURCE=3` makes `read`/`write` `warn_unused_result`, and ⚠⚠ a `(void)` cast does **not** silence that attribute — read the return or use a helper that does (core's eventfd wake/drain is `util_evfd_wake()` / `util_evfd_drain()`). The clean-build set is `-Wall -Wextra -Wpedantic -Wformat-security`; ⛔ `-Wformat=2` is refused **upstream** (skill v1.9.1) because its `-Wformat-nonliteral` fires on every `fmt`-passing wrapper |
| **Find out what is queued, or whether something already shipped** | `TODO.md` §Where the work is (queue state per plugin, one table) → then the plugin's own `TODO.md`. For "did X ship?" read the `ACHIEVED.md` **ledger** — root for cross-cutting, `<plugin>/ACHIEVED.md` otherwise. Never grep the shards for this |
| **Record shipped work / read a chunk's provenance** | Add a row to the nearest `ACHIEVED.md` ledger (chunk · date · **commit SHA** · one-clause outcome · shard). Verbose notes are optional and go in `<dir>/achieved/YYYY-MM.md`; read one with `grep -n '^## <CHUNK-ID>' <dir>/achieved/*.md` and never open a shard whole |
| **Use a CLI tool (`botmanctl`, `ircspy`, `ircspyctl`)** | `tools/AGENTS.md` |
| **Fresh-start the daemon** | `scripts/AGENTS.md` |
| Work on the factory automation (steward, digest, research/family lanes, weekend charter) | `scripts/factory/<lane>.prompt.md` (the operative instructions) + its `scripts/factory/*.service`/`*.timer` pair — **units must be re-installed to `~/.config/systemd/user/` after any edit**. ⚠ Every timer is stopped and deleted (operator 2026-07-28); FACTORY-3/4 are parked in `BACKBURNER.md` and re-authorising unattended runs is an operator decision |
| Add a user command (command-surface plugin) | `CMD.md` + `PLUGIN.md` §Layer Rules (placement decision-tree) |
| Add or work on a misc toy / novelty command (`!math`, `!8ball`) | `plugins/misc/AGENTS.md` |
| Add or modify a plugin layering rule | `PLUGIN.md` §Layer Rules + `AGENTS.md` §Plugin Layers |
| Work on plugin lifecycle / hot-reload, or make a plugin's `deinit()` mirror its `init()` | **`PLUGIN.md §Lifecycle Contract` — authoritative, read it rather than any summary** + `core/plugin.c` (`plugin_audit`, `plugin_reclaim`, `plugin_quiesce`, `plugin_reload`) + `core/cmd.c` (`cmd_unregister_path`). Worklist: `/plugin audit <name>`; daemon-free gate: `scripts/plugin_audit.sh`. The one trap the contract cannot check for you: **a pointer another plugin handed you is invisible to every audit** — register a `plugin_unmap_notify_register()` listener or it kills the daemon later. This is the **normal condition of every service plugin with an async API**, not an edge case: quiesce and audit range-test `curl_iter_req_t.cb`, which for such a service names its own mapping and never the caller's, so the unload proceeds cleanly into the crash. ⭑⭑ **A listener alone is not the whole answer, measured 2026-08-15**: it must clear **every** pointer into the range (a streaming request carries a *chunk* callback as well as a *done* one — `SAN-22` was the second one left behind) and then **wait out the deliveries already running**, since the callback fires on the curl thread under no lock of yours; and because the barrier's range test names the *service's own* mapping, the service must also **end its own in-flight set from `stop()`** via `curl_request_id()` + `curl_request_cancel()` (a cancelled request still delivers, `resp.cancelled` true) or it zombies — `SC-LLM-INFLIGHT`, 4 of 8 `/plugin reload inference` refused. Worked example: `llm_stop()` / `llm_unmap_cb()` / `llm_cancel_user()` in `plugins/extension/inference/engine/llm.c`, consumed by `plugins/bot/chat/extract.c`. **Ten** plugins now carry a listener (`inference/engine`, `whenmoon`, `reachyapi`, `searxng`, `openweather`, `weathergov`, `coinmarketcap`, `coinbase`, `gemini`, `kraken` — recounted live 2026-08-12); copy `reachyapi.c:reachyapi_unmap_cb` (single callback shape, mechanics commented in full) or `coinbase_exchange.c`'s `cb_fwd_*` (six callback shapes collapsed onto one tagged context). `PLUGIN.md §Lifecycle Contract` lists the three things to get right. ⭑ A leaf service whose only caller is its own `_cmd.c` in the same `.so` (`giphy`, `wordnik`, `rawg`, `tmdb`) needs none — and each of the four records why in its `*_api.h`, since the exemption ends the moment another plugin calls in. ⚠ **The other flavour fails silently instead of loudly**: a consumer that tolerates a missing provider via `plugin_dlsym_cached` must **re-resolve on every call and never latch** — core NULLs the slot on unload, so a resolve-once guard blinds the consumer for the life of the daemon (measured, PA-1 `5f4138f`: one `openweather` reload stopped the bot learning locations while the same command still answered). Rules and the worked example: `PLUGIN.md §Pointers core cannot see`. ⭑ **A plugin's reload *snapshot* is the separate question, and it now has a settled answer: persist it, or hold nothing and rebuild in `resume()`.** There is **no core stash and none is planned** (PA-10) — the clause that told plugins to "hand it to core" named a facility core never had, and it cost two chunks before anyone checked. `PLUGIN.md §Optional — suspend() / resume()` carries the ruling and the reason a blob is least trustworthy in exactly the case that motivates a reload; note the tree's only `suspend()`/`resume()` implementor (`plugins/method/reachy/reachy.c:2008`) stores **nothing**, which is the cheapest correct answer when the value need not cross the gap |
| **File a new plugin (which folder?)** | `PLUGIN.md` §Layer Rules (placement decision-tree) |
| Add a service plugin (external API) | `PLUGIN.md` + `plugins/service/AGENTS.md` |
| Wire a consumer to a service's `*_async()` API, or write one | **`include/async.h` is the contract** — every `*_async()` returns `async_rc_t`, and you free your closure on `ASYNC_FAILED_UNDELIVERED` and only that. `PLUGIN.md §Async failure semantics` tables which service refuses which way. ⚠ `ASYNC_AIRBORNE` is `0`, so a bare `!= SUCCESS` still compiles and still means "did not launch" — it just cannot tell you who owns the closure, which is the whole question. Two daemon deaths came from guessing it |
| Add/modify a **method** driver — how a bot meets humans, modality *and* wire (irc, reachy, ...) | `PLUGIN.md` + `plugins/method/AGENTS.md` + `include/method.h` |
| Add/modify the **bot** plugin — the mind that drives a bot (`chat`; there is exactly one) | `PLUGIN.md` + `plugins/bot/AGENTS.md` + `include/bot.h` |
| Add/modify a feature plugin (weather, crypto, stock, urlgrabber, userquote, ...) | `PLUGIN.md` + `plugins/feature/AGENTS.md` + `include/bot.h` |
| Touch **weather** — the `!weather` surface or either provider behind it | The stack is **hybrid** and the routing lives in the feature: `plugins/feature/weather/weather.c` resolves a location (always through openweather's geocoders — weather.gov has none) and chains weather.gov's alerts leg into openweather's conditions leg. Presentation is provider-blind by construction: `weather_adapt.c` turns either provider's structs into the `weather_view_*` types in `weather_view.h`, and `weather_render.c` — which holds **all** presentation, including the alert line's visible-width budget — sees nothing else. Providers: `plugins/service/openweather/` (geocoding, every non-US location, and the floor under every weather.gov leg) and `plugins/service/weathergov/` — one plugin, four TUs: `weathergov.c` (lifecycle, in-flight registry, point/grid lookup + 7-day cache, and the ONLY HTTP/JSON/ISO helpers), `weathergov_alerts.c` (every active CAP alert for a coordinate in ONE GET, deduped and sorted), `weathergov_forecast.c` (the 14 day/night periods **as issued**, plus `wxg_icon_condition_id()` — all 34 icon tokens onto OpenWeather's numbering, the whole condition vocabulary) and `weathergov_obs.c` (stations → latest observation → forecast, one chain, one delivery; ⚠ **every observation value is nullable and observations are always SI**, so the service converts and each field carries a presence flag). ⭑ A caller holding better alerts passes `OPENWEATHER_ALERTS_SKIP` to `openweather_fetch_*`, and openweather's up-to-8 serial `/alert/{id}` GETs never happen. Measured API facts (re-verify, never re-derive): root `TODO.md §WEATHER-1-PLAN` — `§WXG-TRUTH`, `§WEATHER-TRUTH`, `§WEATHER-ALERT-TRUTH`, plus `§WX-NOTPLANNED` (why `!w -h` stays on OpenWeather) and `§WX-FORWARD` (✅ built — the soul's weather chore, `plugins/bot/chat/soul.c`). The chunk plans are shipped and gone; the per-chunk record is the root `ACHIEVED.md` ledger |
| Add/modify an extension subsystem (inference, whenmoon) | `PLUGIN.md` + `plugins/extension/AGENTS.md` |
| Work on the whenmoon plugin C code (markets, candles, backtester, treasuries) | `plugins/extension/whenmoon/AGENTS.md` |
| Act as CFO / touch a treasury or the live disc fund | `plugins/extension/whenmoon/CFO.md` (read FIRST — it is the office) |
| Write, iterate, or backtest a trading strategy (cp1, mako, riptide, ...) | `plugins/extension/whenmoon/strategy/AGENTS.md` |
| Run or interpret a backtest / sweep (the iteration loop) | `plugins/extension/whenmoon/strategy/AGENTS.md` §iteration loop |
| Add/modify an exchange driver (coinbase, gemini, kraken, ...) | `PLUGIN.md` + `plugins/service/AGENTS.md` + `plugins/feature/exchange/AGENTS.md` + that driver's own `plugins/service/<name>/README.md` (auth + protocol specifics) |
| Add/modify a DB driver | `PLUGIN.md` + `plugins/db/AGENTS.md` + `include/db.h` |
| **Write SQL from anywhere** (a query, a mutation, a multi-statement change) | `include/db.h` + `core/AGENTS.md §Patterns` — the bind-parameter and transaction bullet. A value from outside this source file goes in a `db_query_params()` placeholder, not through `db_escape()` + `'%s'`; statements that are one change go in a `db_txn_*` handle, which **pins a pool connection** until it ends and whose `db_txn_commit()` answers FAIL if any statement failed (PostgreSQL reports the COMMIT of an aborted transaction as a success). ⚠ The tree's ~258 existing `db_escape` sites are correct and are **not** being swept — the rule binds new SQL and whatever an edit already walks past |
| Work on a core subsystem | `core/AGENTS.md` + relevant `include/*.h` |
| Add a log/event context (`clam()`) or subscribe to one | `CLAM.md` |
| Work on the inference plugin C code (LLM client, knowledge store, acquisition engine) | `plugins/extension/inference/AGENTS.md` + `plugins/extension/inference/engine/inference.h` |
| Use the LLM client (chat/embed/image/**stt**/**tts**, streaming) | `plugins/extension/inference/LLM.md` + `plugins/extension/inference/engine/inference.h`. The speech kinds are the two odd ones: `stt` sends the engine's only non-JSON body (multipart WAV to `<base>/inference`, so its service URL carries **no** `/v1`) and `tts` receives a binary one (`audio/*` or it is treated as an error) |
| Use the memory subsystem (facts, conversation log, RAG) | `plugins/bot/chat/MEMSTORE.md` + `plugins/bot/chat/memory.h` |
| Work on **what the bot remembers about a person** — how a fact is stored, corrected, named, or written at conversation speed (`/remember`, `/forget`) | `plugins/bot/chat/MEMSTORE.md` **§Merge policies** + **§The fact vocabulary** — then the code: `memory.c` (`memory_upsert_dossier_fact`), `fact_vocab.c` (the key table), `remember.c` (the verbs). Three rules carry the whole design. ⭑ **One observation resolves to affirm / replace / reject by a source ladder** — `admin_seed` > `user_stated` > `llm_extract`/`nl_observe` — higher rank replaces at any confidence, equal rank at `>=` (recency wins ties, because facts describe mutable state), lower rank never replaces a differing value but may affirm an equal one. A rejected observation leaves the row **byte-identical, `last_seen` included**, since prompt injection orders by it. The extractor is clamped to 0.95, so a stored 1.0 is always a human's. ⭑ **`observed_at` refreshes on every accepted observation and nothing else writes it** — that is the entire decay fix, and why the pre-FACT-1 drift-pile still ages out. ⭑ **Keys are canonical or they are lost**: a correction can only land on a row it can find, so `fact_vocab.c` pins key → kind → hint, the extractor's system prompt is *rendered* from it at init, and `/remember` validates against it — free-form keys stay legal everywhere else (1–3 lowercase words, underscores). Moment-state is deliberately **not** a fact (errands, meetings, right-now, anything tied to a day; a dated plan is `upcoming_event:<slug>`, owned by the deferred spine). ⚠ The zip-first weather default (`reply.c` `nl_default_for_type`) can only be exercised when NO location-shaped fact is visible in the prompt — the model bridges with explicit args when it can see one; bury the fact under `rag_top_k/2` newer rows to test it |
| Use the knowledge store (per-persona corpus RAG) | `plugins/extension/inference/KNOWLEDGE.md` + `plugins/extension/inference/engine/inference.h` |
| Use the acquisition engine (autonomous + reactive knowledge learning) | `plugins/extension/inference/ACQUIRE.md` + `plugins/extension/inference/engine/inference.h` |
| Work on the chat bot plugin (command dispatch + LLM conversation) | `plugins/bot/chat/AGENTS.md` + `plugins/bot/chat/CHATBOT.md` |
| Rate-limit, throttle or dedup something **per channel, per nick or per URL** anywhere in `chat` | `plugins/bot/chat/cooldown.h` + `plugins/bot/chat/AGENTS.md` §Concurrency. One slot table serves all five of them (OBS-2, `08-16`); do not hand-roll a sixth. ⭑ **It locks nothing on purpose** — the five instances sit under five different locks (a shared rwlock, three dedicated mutexes, and one shared with three *non*-cooldown siblings), so the caller takes its own and calls `cooldown_ring_peek` / `_stamp` inside it. Matching **folds case** (an IRC channel or nick is case-insensitive per RFC 2812, and three of the five used `strcmp`, so one person under two spellings of their nick held two untouched cooldowns — measured live with a control). Eviction is **LRU**, not the FIFO write cursor two of them ran under a comment claiming otherwise. The reload floor is `peek`'s last argument (TEXT-COOLDOWN-1: a ring dies with the bot handle, and an empty ring reads as *"never spoke here"*) — pass `st->created_at` for a throttle, `0` for a dedup. ⚠ Declare every key buffer `COOLDOWN_KEY_SZ`: a wider key truncates on the stamp and can then never be matched by its own peek, which is silent in both directions. ⛔ **The plugin holds TEN such tables; the other SIX must stay hand-rolled** — engagement (two-part key), CV-4 fallback (a `method_inst_t *` in the key), handoff (a nick as *payload*), and volunteer's channel (two stamps), chunk (`int64_t`) and embed (2048-float vector) rings; they share the find-or-evict shape and nothing else, and that was measured for OBS-2 — don't re-derive it and don't finish the job. Cases: `tests/test_cooldown.c` |
| Work on command dispatch / the identity + auth surface (`identify`, `deauth`, `register`, `id`) | `plugins/bot/chat/dispatch.h` + `plugins/bot/chat/AGENTS.md` §The two halves |
| Turn conversation on or off for a bot | `plugins/bot/chat/AGENTS.md` §The two halves (`bot.<name>.behavior.chat.enabled`) |
| Make a bot answer to a short name (`hh:` for hedgehogg) | `plugins/bot/chat/CHATBOT.md` §Short names — `bot.<name>.aka`, comma-separated, `-` empties it. ⭑ The rule that keeps it safe: a short name addresses the bot at the head of a line, and elsewhere **only when marked** (`@hh`, `hey hh,`), because the bare mid-sentence scan the real nick gets would make a two-letter name answer prose. Per-bot, not per-method; the robot's spoken wake phrases (`bot.<name>.reachy.attention.names`) are a separate mechanism |
| Work on interpreted command output (a persona retelling a tool's reading) | `plugins/bot/chat/CHATBOT.md` §Interpreted command output + `plugins/bot/chat/interpret.c` (capture/settle/cue) + `core/cmd.c` (`cmd_sink_*`, the reply-sink registry the capture rides on). ⭑ **The register of the ask decides**: a banged command gets the renderer's block, prose gets the persona's voice, unconditionally — CARE-4 retired the per-persona `interpret:` opt-in and `METHOD_CAP_SPOKEN`'s force clause with it, so there is no selection logic left to configure |
| Work on **deferred work** — `/in` (run a command later), `/remind`, `/show deferred`, or any chore that needs a durable row | `plugins/bot/chat/CHATBOT.md` §The soul → *The deferred spine* + `plugins/bot/chat/deferred.c`. One table (`chat_deferred`) holds every durable thing the bot owes a human, kinds `say` (speech held until its moment) and `run` (an action held until its moment, dispatched under the asker's stored identity). ⭑ **Permission is checked twice** — `cmd_permits` at schedule time AND again at fire time against the stored tuple, refusal spoken, never silent (the acts-vs-observations rule). Only **NL-capable root** commands are schedulable, uniformly on both invocation paths — that is what keeps `/in` a user feature instead of a way to schedule lifecycle verbs. ⚠ A claim whose method is gone is un-claimed so it delivers late, **except** a repeating row, whose successor the claim statement already wrote — un-claiming that one writes a new successor every tick (measured). ⭑ **A row's moment is a clock or a person**: `due_at` for the first, `deliver_on_presence` for the second (CARE-2 — `soul_on_seen` claims it on a sighting; the venue is the ROW's, so a DM wish stays a DM). The two claim paths are disjoint and share one deliver. The user-facing form is the literal duration **`back`** on either verb (CARE-2b), which resolves to a presence row held disarmed for `back_quiet_secs` first — you are present when you ask. ⚠⚠ **`cmd_nl_t.when` is the ROUTING field**: a `back` clause added there misrouted canonical `/remind` utterances onto `/in` (measured); form goes in `.syntax` + examples only. `expires_at` is written by chores with a deadline (CARE-5: two days) and defaulted to 30 days on a presence row; `repeat_secs` is read by the fire path and written by nobody yet. ⭑ **The row's `source` decides its voice class** (CARE-5): `remind`/`in` are `SOUL_CLASS_TIMED` and can never be refused — the call only records them — while a chore-written row is `UNSOLICITED` and really is gated. Quiet hours are answered *before* the claim, in the presence task; a budget refusal after the claim **returns the row to pending** rather than eating it, because unlike a weather cue a row has somewhere to go back to and its own `expires_at` already says how late is too late. One insert door for everyone: `chatbot_deferred_insert()` |
| Work on the soul (heartbeat, presence, the weather watch, occasions, follow-ups, the price watch, a new chore) | `plugins/bot/chat/CHATBOT.md` §The soul, **§Price watch** (CARE-7 — the only chore whose moment is neither a clock nor a person, and the one that reaches OUT of the plugin: one bulk-ticker snapshot per sweep through `feature/exchange`, admin-curated `chat_watchlist` per bot, one-shot `chat_pricewatch` thresholds per user, and `source='pricewatch'` classed **ASKED** so quiet hours delay the report without the budget ever silencing it. ⚠⚠ Two traps it paid for: the exchange is the "FAIL means the callback ALREADY fired" convention — `PLUGIN.md §Async failure semantics` — so freeing the sweep on the FAIL return is a double free that kills the daemon with no FATAL line; and the NL COMMANDS prompt block is **sorted by name and truncates at the tail**, so adding a command can silently delete a later one from the model's vocabulary — the builder now WARNs, and `CHATBOT_NL_COMMANDS_MAX_BYTES` is the number to raise), **§Occasions** (CARE-5 — the template every fact-driven chore copies: scan a steered fact family → `soul_claim_take()` once per subject-and-occasion in the shared `chat_soul_claims` ledger → write a presence row and let the spine deliver it. The chore itself speaks nothing, which is what keeps venue, restraint and exactly-once out of it) and **§Follow-ups** (CARE-6 — the same template's second instance, and the proof it generalizes: a second date family, no new table, no new throttle, and `source='followup'` classing it UNSOLICITED by construction. Its one addition is calendar arithmetic — `YYYY-MM-DD` or `YYYY-MM`, month-only resolving to the month's LAST day, compared at local **noon** so daylight saving cannot walk a date across the window's edge, and a day the calendar does not have skipped rather than rolled forward), **§The voice budget and quiet hours** (CARE-3 — the ONE throttle every chore calls before it speaks: `soul_voice_permits` in `plugins/bot/chat/voice.c`, classed by *who chose the moment*; a chore that claims durable work must ask `soul_voice_window_open` FIRST, because quiet hours mean later and a spent budget means never. No chore may grow a throttle of its own — weather's `max_per_hour` was retired into this one) **and §The initiative rule** — the latter is binding doctrine on what a chore may speak about unprompted (explicit ask or stated fact only; nothing derived), plus the capability/authorization/disposition split and the acts-vs-observations rule; cite it, don't restate it — + `plugins/bot/chat/soul.c`. ⚠ Every chore knob must join chatbot.c's KV descriptor table — a free-form `bot.*` key refuses `set kv` |
| Score a persona / run the SOUL battery rig | `scripts/soul/` (`spies.sh` holds the IRC presences in a long-lived shell; a round is `reset.sh <persona> && probe.sh <persona> <rN>`; dim-6 halves `remind_probe.sh` / `watch_probe.sh`, output round via `SOUL_ROUND`) + root `TODO.md §SOUL-PLAN` (rubric + scoreboard) |
| **Measure or change semantic recall / retrieval ranking** | `scripts/recall/` — the offline replay rig: `extract.sh <dir>` → `replay.py <dir>` → `judge.py <dir> <arm>`. ⭑⭑ **Use it before the A/B rig, not after.** The whole reply path is reproducible from the DB plus the embed endpoint, so a retrieval question needs **no live bot, no operator and no probe lines** — which is the trap `plugins/bot/chat/TODO.md §CHAT-AB-RIG` exists to warn about (probes pollute the speaker's corpus and outrank real answers within two probes). `replay.py` mirrors `memory_recall_scan_convo()` exactly — self-exclusion, float32 cosine, over-fetch ×3, normalized collapse, `recall_top_k`; change one and it stops measuring production. `judge.py` scores an arm against production **blind and both ways round**, counting only self-consistent verdicts, because position bias is large enough to invent findings. The measured record is `plugins/bot/chat/TODO.md §EMBED-TRUTH` (9 sections) and the code is `plugins/bot/chat/memory_rag.c`. ⚠ The rig was rebuilt from scratch twice before it was kept — it is here so there is not a third time |
| Add/modify conversational admin commands (`/dossier`, `/bot … hush`, `/show bot …`) | `plugins/bot/chat/AGENTS.md` + `plugins/bot/chat/CHATBOT.md` §Admin commands |
| Stand up a new chat bot instance | `plugins/bot/chat/CHATBOT.md` §Creating + `scripts/AGENTS.md` §freshstart.sh |
| Add/modify an output contract | `personalities/AGENTS.md` §contracts + **§The honesty floor and the disposition rule** — every contract that does not already demand a plain "yes, I'm a bot" carries the floor verbatim, and its wording is load-bearing (it was rewritten twice against live persona failures; the section says which sentences and why) |
| Edit a persona's "never claim to be an AI" clause | `personalities/AGENTS.md §The honesty floor and the disposition rule` first. A persona sets *disposition* and may never widen what the mechanism allows: phrase the clause as a deflection, never as an instruction to deny, because the contract floor outranks it and a persona that instructs a flat denial simply gets overruled mid-sentence. Four stock personas (`hands`, `karan`, `pacmanpundit`, `dalebaumhauer`) were reconciled with the floor on 2026-08-11 — match their shape |
| Make a bot's output **bold or coloured** (`!ask` answers, chat replies, any future emitter) | `personalities/AGENTS.md` §Bold and colour (the vocabulary + what a contract should say about it) + `core/colors.c` `color_markup_translate()` — the shared `**bold**` / `<red>…</red>` → `colors.h` marker rewrite, applied by `ask_cmd.c`, `reply.c:send_line_marked` and `claude.c:claude_reply_multi`. ⚠ It is **unconditional** on the chat path: a contract that never mentions markup still gets the model's unprompted Markdown bold rendered instead of leaked. `prompts/ask_default.txt` is the reference wording; `prompts/claude_builtin.txt` is the same contract restated for a dev-facing one-shot bridge |
| Work on the `/claude` bridge / daemon-restart flow | `plugins/extension/inference/claude/AGENTS.md` + `scripts/botman-restart.sh` |
| Work on `!imagine` (text-to-image) or the `image` LLM kind | `plugins/extension/inference/imagine/` (the only backend) + `plugins/extension/inference/LLM.md` §Image generation. The command owns a FIFO with per-caller quotas (`plugin.imagine.max_inflight` / `max_queue` / `max_per_user` / `max_per_anonymous`) and hosts the bytes itself into `output_dir`, served as `public_base`. ⚠ **The engine's reported MIME is always `image/png` and Gemini returns JPEG** — sniff the payload, never the header. The out-of-tree `zimage-service/` that used to back this was retired 2026-08-05 when the GPU went to the robot; its 13 gitignored docs remain on disk and describe a service nobody runs |
| Work on a **leaf service with a command surface** (`!giphy`, `!rawg`, `!tmdb`, `!wotd`/`!dict`, and every future one) | `plugins/service/<name>/` — one plugin, always the same three files: `<name>.c` (provider) + `<name>_cmd.c` (command surface) + `<name>_api.h` (contract). The pattern and when a service may carry commands at all: `plugins/service/AGENTS.md` §Leaf services with a command surface + `PLUGIN.md §Layer Rules` Rule 1 |
| Work on stock/equity/fund/commodity quotes (`!stock`) | `include/stockquote.h` (contract) + `plugins/service/yahoofinance/` (provider) + `plugins/feature/stock/` (command). ⚠ The Yahoo v7 enrichment tier is built but **shipped switched off** — `getcrumb` 429s this public IP; the wait-then-re-test procedure is `TODO.md §STOCK-4-RECHECK` and it is the only open item there |
| Work on per-userns symbol lists (`!stock`/`!crypto` `--list`/`--add`/`--del`, `@name`) | `plugins/feature/stock/stock_lists.c` + `plugins/feature/crypto/crypto_lists.c` — a list is **typed** (one table per class, no `class` column); do not build a shared lists service, and do not re-litigate it (rejected alternatives are in `achieved/2026-07.md §LIST-1 + LIST-2`) |
| Work on `!note` (leave a message for an absent user, delivered when they're next seen) | `plugins/feature/note/AGENTS.md` |
| **Rebuild the Reachy voice stack from scratch** (dead SD card, replaced robot, rebuilt 4090 host, or "what would it take to do this again?") | **`plugins/method/reachy/BRINGUP.md`** — the runbook, in order: fresh image → SSH key → the mic-ribbon trap → earbridge on the robot → whisper/kokorod on the host → LLM registry rows → plugins → bind a bot. It also lists what git does *not* carry (model weights, installed units, DB rows, the `scripts/` helpers). Keep it current in the same commit as any change it describes |
| **Work on the Reachy Mini robot** (body, senses, voice embodiment) | `plugins/method/reachy/TODO.md` — read its §C charter FIRST (measured hardware facts, ratified decisions, the do-not-re-attempt list, and the operator install gates), then the chunk you are executing. Code today: `plugins/service/reachyapi/` (REST mechanism + `reachyapi_api.h`), `plugins/feature/reachycmd/` (**the robot's verbs, scoped to the bot that owns the robot**: `/bot <name> do|**say**|wake|sleep|volume|track|wobble` and `/show bot <name> robot|moves`, offered only to a bot with the `reachy` method bound (`kind_filter`) and **admin-gated by their parent**. `volume`/`track`/`wobble` write `bot.<name>.reachy.*` **before** touching the robot — the KV is the control surface and the robot is a cache of it, re-synchronised on every connect. reachycmd owns **no plugin KV at all**, by construction: a plugin-level row may hold only what is true of every reachy. `say` is synthesize→upload→play over the engine's `tts` kind, voiced by `bot.<name>.reachy.tts_model|tts_voice|tts_speed`; ⚠ **the daemon is ASYMMETRIC about torque** — `goto_sleep` disables the motors itself at the end of its move and `wake_up` never enables them, so `wake` is two hops and `sleep` is one, and a move sent to a limp robot is accepted, returns a uuid, completes and moves NOTHING with no error on any surface. `/show bot <name> robot` reports the mode), `plugins/method/reachy/` (**the method driver** — `/bot addmethod <bot> reachy` gives a chat bot ears: an `reachy_ears_<bot>` persist thread long-polls earbridge, hands each utterance to the engine's `stt` kind, gates it on `bot.<b>.reachy.attention.mode` and `method_deliver`s it into the ordinary chat pipeline; `connect()` stands the robot up only when it finds it down, and `suspend()`/`resume()` exist for one reason — core calls `suspend()` only on the reload path, which is the ONLY way this driver can tell a reload from a `bot stop` and so the only way a reload avoids playing `goto_sleep` over its own wake; and **a mouth**: `send()` queues the reply line into a 16-slot per-bot ring and a `reachy_mouth_<bot>` persist thread synthesizes it through the engine's `tts` kind, uploads it, plays it and paces itself off the WAV's own byte rate so lines never overlap — ⚠ **`barge_in` defaults OFF** because the array's `speech_detected` flag is true in half of a *silent* room, see `ACHIEVED.md §BARGE-TRUTH`), and `senses/` — the out-of-daemon speech hosts, **both live**: `senses/whisper/` (unit + verifying model fetcher for the packaged whisper.cpp STT server on :8004) , `senses/kokorod/` (**ours**, a standalone C Kokoro TTS daemon on :8003 — `POST /v1/audio/speech` → 24 kHz **stereo** WAV peak-normalised to −3 dBFS; both of those output properties are mandatory, see its ledger). Nothing under `senses/` is a plugin or links core; kokorod is meson-guarded on `dependency('sherpa-onnx')` so a tree without the package still builds. Model weights are gitignored. Third host: `senses/earbridge/` — **ours, C, and it runs ON the robot's CM4**, not here (plain Makefile + `deploy.sh`, built remotely; raw mic PCM never leaves the robot over REST). It serves utterances at `http://reachy…:8090/utt?after=&wait=` as 16 kHz mono WAV with `X-Utt-*` headers, and gates each one on the loudest 250 ms window (`--floor-db`, default −35 dBFS) because the XVF3800's speech flag fires on the CM4's own cooling fan and whisper transcribes fan noise into fluent English — **tune that flag by watching `quiet_discards` against `utterances` in `/health`; it is a knob, not a constant, because the fan drifts**. **When the question is "what is the robot actually hearing?", run `scripts/reachy_mic_probe.sh`** — it records raw, VAD-ungated audio off the same shared PCM and hands back a playable WAV plus a level timeline, which is the only way to tell a sensor problem from a transcription problem. ⚠ **Async replies never reach `botmanctl`** — `bctl_reply_target` is cleared the moment dispatch returns, so verify every async robot command over IRC in `#botman` (`tools/ircspy` + `tools/ircspyctl`); botmanctl still proves the *action* (watch `GET /api/move/running` on the robot) |
| Work on the attack duelling game (`!attack`, `!heal`, `!defer`, `show attack`; character sheets in `plugins/feature/attack/characters/`) | `plugins/feature/attack/AGENTS.md` |
| Work on the one-shot LLM command (`!ask`) | `plugins/extension/inference/ask/` + `plugins/extension/inference/LLM.md` |
| Work on the search commands (`!search`, `!image`, `!news`, `!video`, `!music`) | `plugins/extension/inference/search/searxng_cmd.c` (command surface) + `plugins/service/searxng/searxng_api.h` (mechanism). ⭑ A search returning **exactly one** result offers that URL to the title-grabber via the `url_offer` clam event (`CLAM.md §Cross-plugin contracts`) — a multi-result search offers nothing, since a title per line would bury the results. It is an event and not a call because an extension may not depend on a feature |
| Merge a command surface into its leaf service | `plugins/service/AGENTS.md` §Leaf services with a command surface + `PLUGIN.md §Layer Rules` Rule 1 |
| Work with the IRC method driver or its identity projection | `plugins/method/AGENTS.md` + `plugins/method/irc/AGENTS.md` + `plugins/method/irc/irc_identity.h`. ⭑ **Line parsing is `irc_parse.c`, on its own and tested** (`tests/test_irc_parse.c`): what it fills is what `bot_identity_resolve()` reads, so it **refuses** a line whose prefix, nick, user or host did not fit rather than shortening one — `irc_protocol.c` is the formatters only |
| Ingest or refresh a knowledge corpus | `plugins/extension/inference/KNOWLEDGE.md` + `scripts/AGENTS.md` §fetch_archwiki.sh |
| Work on the dossier subsystem / `/dossier` admin commands | `plugins/bot/chat/DOSSIER.md` + `plugins/bot/chat/dossier.h` |
| Work on LLM fact extraction (dossier_facts) | `plugins/bot/chat/FACT_EXTRACT.md` + `plugins/bot/chat/extract.h` |
| Edit or add a chat personality | `personalities/AGENTS.md` + `plugins/bot/chat/CHATBOT.md` |
| Build system changes | `BUILD.md` + root `meson.build` + relevant subdir `meson.build` |
| **Run the tests, or add a case to them** | `tests/meson.build` + the suite you need — `tests/test_util.c` (`util_redact_url`, `util_url_is_safe_https`, `util_b64_*`), `tests/test_colors.c` (`color_markup_translate`), `tests/test_json.c` (`json_unescape` — the LLM stream's escape decoder), `tests/test_irc_parse.c` (`irc_parse_line` — the fields an identity is resolved from), `tests/test_fact_merge.c` (the FACT-1 merge ladder), `tests/test_db_txn.c` (`db_query_params` + the `db_txn_*` trio, driven through a driver that runs no SQL and only records — **no database needed**, unlike its `fact_merge` neighbour). Run: `ninja -C build && cd build && meson test`. ⭑ **The suite is deliberately tiny and stays that way** — root `TODO.md` axes A27/A28/A31 filtered it to **silent-wrongness surfaces only** (a secret in a log, a fetch of a private address, forged formatting, a person's fact quietly overwritten). Loud, recoverable, plugin-local failures are untested **by design**; do not "improve" coverage by adding cases for them. Each surface is one table under one loop. ⚠⚠ **`test_fact_merge` needs Postgres** because the ladder *is* SQL: it renders `MEM_FACT_OBSERVE_SQL` from `plugins/bot/chat/memory.h` — the same template `memory.c` sends, which is why the macro lives in the header — against a **TEMP `dossier_facts` that shadows the real table**, with `search_path = pg_temp` and a `ROLLBACK`. It reads `botman.conf` itself and **SKIPs (exit 77)** when the DB is unreachable, so a green run is not proof it ran; check the log for `52 checks`. ⭑ A new case must be shown to **fail** before it is trusted — mutate the thing under test, watch that one row go red, revert |
| Understand overall architecture | `DESIGN.md` |
| Find a header's purpose | `include/AGENTS.md` |
